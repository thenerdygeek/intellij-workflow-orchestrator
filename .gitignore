# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
!**/src/main/**/build/
!**/src/test/**/build/

# IntelliJ
.idea/
*.iml
*.iws
*.ipr
out/
.intellijPlatform/

# Claude — ignore everything under .claude/ except team-shared hooks + settings
.claude/*
!.claude/hooks/
!.claude/settings.json

# Node
agent/webview/node_modules/

# Webview build output (generated by Vite)
agent/src/main/resources/webview/dist/

# OS
.DS_Store
Thumbs.db

# Kotlin
*.class
.kotlin/

# Python
__pycache__/
*.pyc

# Playwright
.playwright-mcp/

# Screenshots / mockups in project root
/*.png

# Temporary snapshots
/flow-snapshot.md

# Test/lab result artifacts
docs/Result_*/
tools/sourcegraph-probe/Result_*/
# Atlassian probe — captures contain redacted-but-still-internal API
# response data + plan/build/result keys; never commit (per user directive
# 2026-05-08). Earlier commits 5d2961e3 / ff332fbc / 3e0c55ff landed
# bundle*.txt files; those have been untracked from the working tree but
# remain in history. Run `git filter-repo --invert-paths --path-glob
# 'tools/atlassian-probe/Result_*'` + force-push if a clean history is
# required (destructive — anyone who pulled this branch will need to reset).
tools/atlassian-probe/Result_*/

# Superpowers plans (ephemeral dev artifacts)
docs/superpowers/plans/

# Vite
agent/webview/.vite/
agent/webview/tsconfig.tsbuildinfo

# API probe (config contains tokens, results contain API data)
scripts/api-probe-config.json
scripts/api-probe-results/

# Plugin
/run/
.superpowers/
.worktrees/
.claire/
hs_err_pid*.log
*.hprof

# Temporary API-doc extraction source — probe bundles copied from the root repo (Task 7), removed in Task 9
/.apidocs-source/

# Maestro skill artifacts
/.maestro/
