package com.workflow.orchestrator.agent.brain

class CodyContextService {
    suspend fun searchCode(query: String): String {
        return "Cody context service not yet connected. Use file_structure and search_code tools for code exploration."
    }
}
