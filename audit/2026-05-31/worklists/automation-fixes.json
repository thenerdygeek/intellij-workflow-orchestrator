[
  {
    "id": "AUTOMATION-COR-1",
    "module": "automation",
    "tab": "Automation",
    "lens": "correctness",
    "category": "bug",
    "severity": "High",
    "title": "cancel() silently skips Bamboo stop for RUNNING builds \u2014 build keeps running after user cancels",
    "file": "automation/src/main/kotlin/com/workflow/orchestrator/automation/service/QueueService.kt",
    "description": "QueueService.cancel() only fires bambooService.cancelBuild() when the entry status is QUEUED_ON_BAMBOO. RUNNING entries (which have already started executing on Bamboo) are silently transitioned to CANCELLED locally without any Bamboo API call. cancelBuild maps to DELETE /queue/{key} (removes a queued-but-not-started build); the separate stopBuild maps to PUT /result/{key}/stop (terminates an in-progress build). Neither is called for RUNNING entries. The result: the user clicks Cancel on a running build, the Monitor list clears the row and shows CANCELLED, but the Bamboo agent continues executing the job until completion. This wastes CI resources and misleads the user about the build's actual state.",
    "evidence": "// QueueService.kt lines 212-216\nval resultKey = entry.bambooResultKey\nif (resultKey != null && entry.status == QueueEntryStatus.QUEUED_ON_BAMBOO) {\n    log.info(\"[Automation:Queue] Cancelling Bamboo build $resultKey\")\n    bambooService.cancelBuild(resultKey)\n}",
    "suggestedFix": "In the cancel() mutex block, extend the status guard to also call bambooService.stopBuild(resultKey) for RUNNING entries: `if (resultKey != null) { when (entry.status) { QueueEntryStatus.QUEUED_ON_BAMBOO -> bambooService.cancelBuild(resultKey); QueueEntryStatus.RUNNING -> bambooService.stopBuild(resultKey); else -> {} } }`. Both calls should log and silently continue on error (the local CANCELLED transition is still correct \u2014 if the build finishes milliseconds before the stop reaches Bamboo, the poll loop will detect the terminal state on the next tick). Add a test: enqueue an entry, advance it to RUNNING status, call cancel(), and verify stopBuild was called exactly once.",
    "verdictReasoning": "Confirmed real. QueueService.kt lines 212-216 show the guard `entry.status == QueueEntryStatus.QUEUED_ON_BAMBOO` \u2014 RUNNING entries skip the Bamboo API call entirely. `bambooService.stopBuild()` (PUT /result/{key}/stop for in-progress builds) is implemented in BambooServiceImpl.kt (line 411), is on the BambooService interface (line 75), and is used in BuildDashboardPanel.kt (line 962) for the same purpose from the Bamboo tab. The omission for RUNNING queue entries is a clear logic gap: the user sees CANCELLED locally but the Bamboo agent continues running. The fix is straightforward and does not break any load-bearing path. `cancelBuild` maps to DELETE /queue/{key} and would be wrong/no-op for an already-running build anyway, so calling it for RUNNING would also be incorrect \u2014 calling `stopBuild` for RUNNING is the architecturally correct fix.",
    "recommendedAction": "In QueueService.cancel(), extend the status check: for RUNNING entries call bambooService.stopBuild(resultKey), for QUEUED_ON_BAMBOO entries call bambooService.cancelBuild(resultKey). Both should log the call. The local CANCELLED transition proceeds regardless.",
    "panel": null
  },
  {
    "id": "AUTOMATION-ARC-1",
    "module": "automation",
    "tab": "Automation",
    "lens": "architecture",
    "category": "architecture",
    "severity": "High",
    "title": "Direct cross-module dependency on :bamboo UI package",
    "file": "automation/src/main/kotlin/com/workflow/orchestrator/automation/ui/AutomationPanel.kt",
    "description": "AutomationPanel imports and directly instantiates `ManualStageDialog` and uses `TriggerMode` from `com.workflow.orchestrator.bamboo.ui` \u2014 a sibling feature module. The project convention is that feature modules depend ONLY on `:core`; cross-module interaction must go through `:core` interfaces or EventBus. The `:automation` build.gradle.kts already lists `implementation(project(\":bamboo\"))`, making the compile-level coupling explicit and creating a directed dependency cycle risk if `:bamboo` ever needs `:automation` types.",
    "evidence": "import com.workflow.orchestrator.bamboo.ui.ManualStageDialog\nimport com.workflow.orchestrator.bamboo.ui.TriggerMode\n...\nval dialog = ManualStageDialog(project = project, planKey = currentSuitePlanKey, scope = scope, triggerMode = TriggerMode.CUSTOM_STAGES, ...)",
    "suggestedFix": "Move `ManualStageDialog` and `TriggerMode` to `:core` (e.g. `core/ui/bamboo/` or `core/model/`), or define a `StagePickerLauncher` extension point in `:core` (similar to `createPrLauncher` / `openPrLister`) that `:bamboo` registers and `:automation` calls through the EP. Either approach severs the direct `:automation \u2192 :bamboo` compile dependency and lets the build.gradle.kts drop `implementation(project(\":bamboo\"))` entirely.",
    "verdictReasoning": "Confirmed: AutomationPanel.kt lines 25-26 import com.workflow.orchestrator.bamboo.ui.ManualStageDialog and TriggerMode. Lines 1002-1009 instantiate ManualStageDialog directly. The automation/build.gradle.kts line 75 declares implementation(project(':bamboo')). The bamboo module has no dependency on automation (no output from reverse grep), so there is no cycle yet, but the :automation -> :bamboo compile dependency violates the stated convention that feature modules depend ONLY on :core. This is a real architectural violation. ManualStageDialog is defined in the bamboo.ui package (not core), and TriggerMode is co-located there. Fixing requires either moving ManualStageDialog/TriggerMode to :core or introducing an extension-point seam in :core.",
    "recommendedAction": "Move ManualStageDialog and TriggerMode to :core (e.g., core/ui/bamboo/ subpackage) or define a StagePicker extension point in :core that :bamboo registers and :automation invokes. Either approach removes the direct :automation -> :bamboo compile dependency. Update build.gradle.kts to drop implementation(project(':bamboo')). Medium effort; prioritize to avoid the dependency creeping further.",
    "panel": null
  },
  {
    "id": "AUTOMATION-COR-2",
    "module": "automation",
    "tab": "Automation",
    "lens": "correctness",
    "category": "bug",
    "severity": "Medium",
    "title": "cancel() result from bambooService silently ignored \u2014 cancellation failure is invisible to the user",
    "file": "automation/src/main/kotlin/com/workflow/orchestrator/automation/service/QueueService.kt",
    "description": "At line 215, bambooService.cancelBuild(resultKey) is called but its ToolResult<Unit> return value is completely discarded. If Bamboo returns an auth failure (403), plan not found (404), or network error, the entry is still transitioned to CANCELLED locally and the EventBus emits QueuePositionChanged(-1). The user has no indication the cancel request failed, and Bamboo's queue still holds the build. The same pattern is present for the not-yet-implemented stopBuild gap (COR-1). In contrast, handleWaitingLocal carefully classifies trigger failures by ErrorType and distinguishes permanent vs transient to decide whether to enter FAILED_TO_TRIGGER.",
    "evidence": "// QueueService.kt line 215 \u2014 return value of suspend fun cancelBuild() is ignored\nbambooService.cancelBuild(resultKey)",
    "suggestedFix": "Capture the result and log it: `val cancelResult = bambooService.cancelBuild(resultKey); if (cancelResult.isError) { log.warn(\"[Automation:Queue] Failed to cancel build $resultKey on Bamboo (continuing with local CANCELLED): ${cancelResult.summary}\") }`. For a better UX, surface the Bamboo error in the entry's errorMessage field so the detail panel shows it. The local CANCELLED transition should still proceed regardless (the user's intent is to remove this entry from their view).",
    "verdictReasoning": "Confirmed real. Line 215 of QueueService.kt calls `bambooService.cancelBuild(resultKey)` and discards the `ToolResult<Unit>` return value. If Bamboo returns 403/404 or a network error, the entry is still locally transitioned to CANCELLED and the EventBus emits QueuePositionChanged(-1). The user has no visible indication that Bamboo still holds the build. By contrast, handleWaitingLocal (lines 400-423) carefully classifies failures via ErrorType before deciding on terminal vs retry. Logging the error (and optionally surfacing it in `errorMessage`) would be an improvement with no risk of breaking existing behavior. The local transition to CANCELLED is intentionally unconditional (user's intent to remove the row), so fix is additive.",
    "recommendedAction": "Capture `val cancelResult = bambooService.cancelBuild(resultKey)` and log a warning if `cancelResult.isError`. Optionally populate `entry.errorMessage` so the Monitor detail panel surfaces it.",
    "panel": null
  },
  {
    "id": "AUTOMATION-COR-3",
    "module": "automation",
    "tab": "Automation",
    "lens": "correctness",
    "category": "bug",
    "severity": "Medium",
    "title": "enqueue() fast-path holds the mutex across two Bamboo HTTP round-trips, blocking all concurrent user actions",
    "file": "automation/src/main/kotlin/com/workflow/orchestrator/automation/service/QueueService.kt",
    "description": "Inside mutex.withLock { } in enqueue(), the fast-path (lines 170\u2013191) makes two sequential Bamboo HTTP calls \u2014 bambooService.getRunningBuilds() then doTrigger() (which calls bambooService.triggerBuild()) \u2014 while holding the coroutine Mutex. Any concurrent cancel(), dismiss(), or pollOnce() call that also tries to acquire the mutex will suspend for the full round-trip latency of both HTTP calls (potentially 2\u201310+ seconds each on a slow VPN). The pollOnce() was explicitly redesigned (comment at line 345) to avoid exactly this pattern by snapshotting IDs under the lock and re-acquiring per-entry. The enqueue fast-path was not given the same treatment and creates the same 'block everything while HTTP is in-flight' freeze that the pollOnce redesign was meant to fix.",
    "evidence": "// QueueService.kt lines 136-191 \u2014 mutex.withLock spans getRunningBuilds + doTrigger\nmutex.withLock {\n    ...\n    val runningResult = bambooService.getRunningBuilds(entry.branchKey ?: entry.suitePlanKey) // HTTP 1\n    if (!runningResult.isError && runningResult.data!!.isEmpty()) {\n        val triggerResult = doTrigger(entry) // HTTP 2 (triggerBuild inside)\n        ...\n    }\n}",
    "suggestedFix": "Apply the same snapshot-release-reacquire pattern used in pollOnce: release the mutex after adding the entry to _stateFlow and emitting QueuePositionChanged; then check and trigger outside the lock. Atomicity for the state mutation (adding to _stateFlow) is still correct; the idle-check and trigger can be done without the lock since they only observe Bamboo state and then re-acquire to update the entry's status. Example restructuring: hold the lock only for the state-mutation block (lines 147\u2013165), break out of the lock, then do the idle-check+trigger outside, and re-acquire a new lock to update the entry's bambooResultKey if the trigger succeeds.",
    "verdictReasoning": "Confirmed real. QueueService.kt lines 136-192 show `mutex.withLock { ... }` wrapping lines 136-192, which includes the call to `bambooService.getRunningBuilds(...)` (HTTP call 1, line 174) and `doTrigger(entry)` (HTTP call 2 via triggerBuild, line 177), all while holding the mutex. The comment at lines 345-355 on pollOnce explicitly documents the prior pattern as Flaw #8 and describes the snapshot-release-reacquire fix. The enqueue fast-path was not given the same treatment. Any concurrent cancel(), dismiss(), or pollOnce() that acquires the mutex will block for the full latency of both HTTP calls (potentially 2-10+ seconds on VPN). Severity is Medium not High because this is a responsiveness issue, not data loss \u2014 the fast-path only executes when `suiteEntries == 0`, limiting how often it's triggered.",
    "recommendedAction": "Apply the same snapshot-release pattern: release the mutex after adding the entry to _stateFlow and emitting QueuePositionChanged (around line 165), then perform the idle-check and doTrigger outside the lock, then re-acquire a new lock to update the entry's bambooResultKey.",
    "panel": null
  },
  {
    "id": "AUTOMATION-COR-4",
    "module": "automation",
    "tab": "Automation",
    "lens": "correctness",
    "category": "bug",
    "severity": "Medium",
    "title": "onBuildLogReady() reads non-@Volatile field currentSuitePlanKey from IO dispatcher \u2014 JMM data race",
    "file": "automation/src/main/kotlin/com/workflow/orchestrator/automation/ui/AutomationPanel.kt",
    "description": "subscribeToBuildEvents() launches a coroutine on scope (Dispatchers.IO) that calls onBuildLogReady() in the collect{} body. onBuildLogReady() reads currentSuitePlanKey at lines 787\u2013788 without any synchronization. currentSuitePlanKey is a plain var (no @Volatile) written on the EDT by onSuiteSelected() (line 450) and the init block (line 381). Under the Java Memory Model, a write on the EDT and a read on an IO thread to an unsynchronized field have no happens-before ordering \u2014 the IO thread may see a stale (empty-string) value. In the worst case, the BlankShield at line 787 evaluates isBlank() on the stale empty string and drops a valid BuildLogReady event, leaving the docker-tag banner stuck in the 'Waiting for CI build...' state even after the build completes. By contrast, currentFocusBuild is correctly annotated @Volatile (line 189-190).",
    "evidence": "// AutomationPanel.kt line 184 \u2014 plain var, no @Volatile\nprivate var currentSuitePlanKey: String = \"\"\n\n// AutomationPanel.kt lines 786-788 \u2014 read on IO thread (subscribeToBuildEvents scope)\nprivate fun onBuildLogReady(event: WorkflowEvent.BuildLogReady) {\n    if (currentSuitePlanKey.isBlank()) {  // data race: IO reads EDT-written field",
    "suggestedFix": "Add @Volatile to currentSuitePlanKey (line 184): `@Volatile private var currentSuitePlanKey: String = \"\"`. Since it only stores a String (a reference type), @Volatile gives the necessary JMM visibility guarantee with negligible overhead. Alternatively, capture the value on the EDT before launching the IO coroutine and pass it as a parameter, but @Volatile is simpler and consistent with the existing @Volatile currentFocusBuild pattern.",
    "verdictReasoning": "Confirmed real. AutomationPanel.kt line 184 shows `private var currentSuitePlanKey: String = \"\"` \u2014 no @Volatile annotation. The field is written on the EDT by onSuiteSelected() (line 450) and the init block (line 381). It is read in onBuildLogReady() (line 787), which is called from a `scope.launch { eventBus.events.collect { ... } }` coroutine (lines 776-784) \u2014 that scope is `CoroutineScope(Dispatchers.IO + SupervisorJob())` (line 57). Under the JMM, the IO-thread read has no happens-before ordering with the EDT write. This can cause a stale empty-string read, dropping a valid BuildLogReady event. The comparison with `@Volatile currentFocusBuild` (lines 189-190) confirms the pattern is known and intentional for that field \u2014 it was simply missed for `currentSuitePlanKey`. Adding `@Volatile` is a single-line fix with negligible overhead.",
    "recommendedAction": "Add `@Volatile` to the `currentSuitePlanKey` declaration at line 184, consistent with the existing `@Volatile currentFocusBuild` pattern.",
    "panel": null
  },
  {
    "id": "AUTOMATION-ARC-3",
    "module": "automation",
    "tab": "Automation",
    "lens": "architecture",
    "category": "architecture",
    "severity": "Medium",
    "title": "dockerTagsJson content partially logged at INFO \u2014 information-disclosure risk",
    "file": "automation/src/main/kotlin/com/workflow/orchestrator/automation/service/TagBuilderService.kt",
    "description": "Line 149 logs `dockerTagsJson.take(100)` at `log.info` level when the parsed JSON is empty. The existing code comment at line 279 (`buildJsonPayload`) explicitly bans logging payload content ('Full service\u2192tag mappings may include internal registry paths and version fingerprints'). The take(100) truncation is insufficient: a single JSON entry like `{\"myservice\":\"repo.internal.corp/svc:1.2.3-build.4567\"}` is 57 chars, well within 100, leaking both the internal registry hostname and the Docker image reference into `idea.log` on every build where the parse fails (e.g. the first few builds of a plan before dockerTagsAsJson appears).",
    "evidence": "val reason = \"#${build.buildNumber}: dockerTagsAsJson parsed to empty \u2014 ${dockerTagsJson.take(100)}\"\nlog.info(\"[Automation:Tags]   $reason\")\nskippedReasons.add(reason)",
    "suggestedFix": "Replace `${dockerTagsJson.take(100)}` with a non-content diagnostic: log the raw string length and a schema hint instead, e.g. `\"dockerTagsAsJson was ${dockerTagsJson.length} chars, did not parse as a non-empty JSON object\"`. This matches the approach already taken in `buildJsonPayload` (logs `entries.size` and `length`, never the content). The same fix should be applied to the `skippedReasons` string that is later surfaced in the diagnostic banner \u2014 the banner text will leak into the UI otherwise.",
    "verdictReasoning": "Confirmed by reading TagBuilderService.kt line 149: the reason string logged at INFO is '#${build.buildNumber}: dockerTagsAsJson parsed to empty \u2014 ${dockerTagsJson.take(100)}'. The take(100) truncation does not prevent leaking internal registry hostnames or Docker image references \u2014 a typical entry like {'myservice':'repo.internal.corp/svc:1.2.3-build.4567'} is ~57 chars, well within 100. This contradicts the explicit project policy documented at line 279 in the same file: 'Log metadata only \u2014 never log payload content'. The same content is added to skippedReasons (line 151) which is surfaced in the UI diagnostic banner. The fix (log length and schema hint instead of content) matches the existing pattern in buildJsonPayload (lines 279-282) and has no risk of breaking functionality.",
    "recommendedAction": "Replace '${dockerTagsJson.take(100)}' at line 149 with a non-content diagnostic such as 'dockerTagsAsJson was ${dockerTagsJson.length} chars, did not parse as a non-empty JSON object'. Apply the same replacement to the skippedReasons string to prevent leaking content into the UI banner. This matches the approach already used in buildJsonPayload.",
    "panel": null
  },
  {
    "id": "AUTOMATION-COR-5",
    "module": "automation",
    "tab": "Automation",
    "lens": "correctness",
    "category": "bug",
    "severity": "Low",
    "title": "TagHistoryService connection leaked when dispose() races with lazy initialization",
    "file": "automation/src/main/kotlin/com/workflow/orchestrator/automation/service/TagHistoryService.kt",
    "description": "TagHistoryService.close() guards the connection close with `if (!connectionInitialized) return` at line 335. The `connectionInitialized` flag is set to `true` at line 60, inside the lazy { } block, after the Connection is opened and initSchema is run. If dispose() is called (from the EDT on project close) while a background coroutine is mid-way through the lazy initialization block (connection created, initSchema running, but connectionInitialized not yet set to true), close() sees connectionInitialized=false and returns early. The lazy block then completes, leaves connectionInitialized=true, and the Connection is never closed. The leaking connection holds WAL/SHM file locks on the SQLite DB, causing 'database is locked' errors if the project is immediately reopened in another IDE window. Kotlin's by lazy uses LazyThreadSafetyMode.SYNCHRONIZED (prevents double-init), but the connectionInitialized flag is set at the end of the block, creating a window where the connection object exists but the guard flag is still false.",
    "evidence": "// TagHistoryService.kt lines 41-62\n@Volatile\nprivate var connectionInitialized = false\n\nprivate val connection: Connection by lazy {\n    ...\n    connectionInitialized = true  // set at END of block; window before this = leak risk\n    conn\n}\n\n// TagHistoryService.kt line 335\nfun close() {\n    if (!connectionInitialized) return  // may be false while lazy block is mid-execution\n    try { connection.close() } catch (_: Exception) {}\n}",
    "suggestedFix": "Eliminate the separate connectionInitialized flag and instead use the lazy property's own initialization state. Replace the guard in close() with a null-safe access pattern: declare `private val connectionOrNull: Connection? get() = if (::connection.isInitialized) connection else null` (Kotlin lateinit form), or change to `private val _connection: Connection? by lazy { ... }` returning null on failure. Then close() becomes: `_connection?.let { try { it.close() } catch (_: Exception) {} }`. This removes the TOCTOU window between the Connection being created and the flag being set.",
    "verdictReasoning": "Confirmed real in theory. TagHistoryService.kt shows `connectionInitialized = true` is set at line 60, at the END of the lazy block (after initSchema runs). `close()` at line 334 guards on `if (!connectionInitialized) return`. Kotlin lazy uses SYNCHRONIZED mode by default, meaning only one thread can execute the lazy block at a time. However, if `dispose()` is called while the lazy block is mid-execution (e.g., initSchema is running and connectionInitialized is still false), `close()` returns early without closing the connection. After the lazy block completes, the connection is open but will never be closed. This can cause WAL file lock issues on project reopen. That said, the window is very narrow in practice (project close racing with first DB access), and the service is project-scoped so it's rarely a real-world issue. Severity is Low (as claimed) \u2014 edge case, not a normal-use bug.",
    "recommendedAction": "Move `connectionInitialized = true` to BEFORE `conn` is returned, or better, eliminate the flag and use a nullable `_connection` val. The suggested null-safe approach in the finding is sound.",
    "panel": null
  },
  {
    "id": "AUTOMATION-CLE-1",
    "module": "automation",
    "tab": "Automation",
    "lens": "cleanup",
    "category": "cleanup",
    "severity": "Low",
    "title": "resolveServiceCiPlanKey() is a private method with zero call sites",
    "file": "automation/src/main/kotlin/com/workflow/orchestrator/automation/ui/AutomationPanel.kt",
    "description": "The private method `resolveServiceCiPlanKey()` at line 715 has no callers anywhere in the codebase (confirmed via `rg` across the entire worktree). The KDoc on line 713 explicitly states \"NOTE (Phase B): This method is no longer called from the docker-tag detection path\" and promises a Phase D cleanup that has not happened. The method is 13 lines of non-trivial logic that is simply dead.",
    "evidence": "private fun resolveServiceCiPlanKey(): String {\n    val fromDedicated = settings.state.serviceCiPlanKey?.takeIf { it.isNotBlank() }\n    if (fromDedicated != null) return fromDedicated\n    ...\n}\n// Only occurrence at line 715 \u2014 zero call sites in production or test code",
    "suggestedFix": "Delete `resolveServiceCiPlanKey()` entirely (lines 715-727). The note in the KDoc already calls this a pending cleanup.",
    "verdictReasoning": "Confirmed real. Grep confirms `resolveServiceCiPlanKey` appears only at its definition site (AutomationPanel.kt line 715) \u2014 zero call sites. The KDoc at line 710-713 explicitly acknowledges it is no longer called and notes a Phase D cleanup that has not happened. The method is 13 lines of dead code. Deleting it is safe and does not affect any other functionality. Severity is Low \u2014 dead code, no behavior impact.",
    "recommendedAction": "Delete resolveServiceCiPlanKey() at lines 715-727.",
    "panel": null
  },
  {
    "id": "AUTOMATION-CLE-2",
    "module": "automation",
    "tab": "Automation",
    "lens": "cleanup",
    "category": "cleanup",
    "severity": "Low",
    "title": "DriftDetectorService and its reconcileFromBamboo guard block are permanently dead",
    "file": "automation/src/main/kotlin/com/workflow/orchestrator/automation/ui/AutomationPanel.kt",
    "description": "`DriftDetectorService.isRegistryConfigured()` unconditionally returns `false` (by design \u2014 registry calls removed). The guard in `reconcileFromBamboo` at lines 622\u2013625 therefore can never execute. The three lines inside the `if` block, the `driftDetectorService` lazy field (line 63), and the `DriftDetectorService` class itself are all permanently dead complexity. The service is still registered in `plugin.xml` (line 306), adding pointless DI overhead.",
    "evidence": "// DriftDetectorService.kt:25\nfun isRegistryConfigured(): Boolean = false\n\n// AutomationPanel.kt:622-625\nif (tags.isNotEmpty() && driftDetectorService.isRegistryConfigured()) {\n    log.info(\"[Automation:UI] Fetching latest release tags from registry...\")\n    tags = driftDetectorService.enrichWithLatestReleaseTags(tags)\n}",
    "suggestedFix": "Delete the `if (tags.isNotEmpty() && driftDetectorService.isRegistryConfigured())` block in `reconcileFromBamboo`. Remove the `driftDetectorService` lazy field from `AutomationPanel`. Delete the entire `DriftDetectorService` class. Remove its `<projectService>` registration from `plugin.xml`.",
    "verdictReasoning": "Confirmed real. DriftDetectorService.kt line 25 shows `fun isRegistryConfigured(): Boolean = false` \u2014 unconditional. The guard in AutomationPanel.kt lines 622-625 (`if (tags.isNotEmpty() && driftDetectorService.isRegistryConfigured())`) is therefore permanently dead \u2014 it never executes. The service KDoc in DriftDetectorService.kt confirms this is by design: 'registry calls removed'. The `driftDetectorService` lazy field (line 63), the service class, and its plugin.xml registration (line 306) are all permanently dead weight. However, severity should be Low rather than Medium: the code compiles, runs correctly (the no-op path is fine), and there is no behavior impact \u2014 this is purely a dead-code cleanup. The comment in automation/CLAUDE.md confirms `DriftDetectorService \u2014 no-op after registry calls removed; isRegistryConfigured() always returns false`.",
    "recommendedAction": "Delete the `if (tags.isNotEmpty() && driftDetectorService.isRegistryConfigured())` block, the `driftDetectorService` lazy field from AutomationPanel, the DriftDetectorService class, and its projectService registration from plugin.xml.",
    "panel": null
  },
  {
    "id": "AUTOMATION-CLE-3",
    "module": "automation",
    "tab": "Automation",
    "lens": "cleanup",
    "category": "cleanup",
    "severity": "Low",
    "title": "DriftResult model and checkDrift() are completely unused dead types",
    "file": "automation/src/main/kotlin/com/workflow/orchestrator/automation/model/AutomationModels.kt",
    "description": "`DriftResult` (lines 111\u2013116) is a `data class` that appears only in the no-op `DriftDetectorService.checkDrift()` signature and one test that just constructs the object. `checkDrift()` is never called from production code. With `DriftDetectorService` dead (CLE-2), `DriftResult` is unreachable type pollution in the public model namespace.",
    "evidence": "data class DriftResult(\n    val serviceName: String,\n    val currentTag: String,\n    val latestReleaseTag: String,\n    val isStale: Boolean\n)\n// Only production use: DriftDetectorService.checkDrift() return type \u2014 which is never called",
    "suggestedFix": "Delete `DriftResult` from `AutomationModels.kt` and delete the corresponding test case in `AutomationModelsTest.kt` (`DriftResult marks stale when versions differ`).",
    "verdictReasoning": "Confirmed real. AutomationModels.kt lines 111-116 show `data class DriftResult(...)`. Grep confirms it is only used in DriftDetectorService.checkDrift() return type (which is never called from production code) and one test in AutomationModelsTest.kt lines 120-128 that only constructs the object. With DriftDetectorService being dead (as established in CLE-2), DriftResult is unreachable type pollution. Deleting it along with the test case is safe. Severity is Low \u2014 dead type, no behavior impact.",
    "recommendedAction": "Delete DriftResult from AutomationModels.kt (lines 111-116) and delete the corresponding test case in AutomationModelsTest.kt (lines 119-128).",
    "panel": null
  },
  {
    "id": "AUTOMATION-CLE-4",
    "module": "automation",
    "tab": "Automation",
    "lens": "cleanup",
    "category": "cleanup",
    "severity": "Low",
    "title": "TagEntry.isDrift and RegistryStatus.{VALID,NOT_FOUND,CHECKING} are permanently unreachable UI states",
    "file": "automation/src/main/kotlin/com/workflow/orchestrator/automation/model/AutomationModels.kt",
    "description": "In production, `TagEntry.isDrift` is always `false` and `TagEntry.registryStatus` is always `RegistryStatus.UNKNOWN` (confirmed by searching all assignments in `main/`). The renderer in `TagStagingPanel` has live branches for `isDrift -> WARNING`, `VALID -> SUCCESS`, `NOT_FOUND -> ERROR`, and `CHECKING`, but these branches can never fire at runtime. The values `VALID`, `NOT_FOUND`, and `CHECKING` in the `RegistryStatus` enum are dead. Similarly `TagEntry.latestReleaseTag` is always `null`.",
    "evidence": "// All production assignments (TagBuilderService.kt, BaselineCacheService.kt, TagStagingPanel.kt):\nregistryStatus = RegistryStatus.UNKNOWN\nisDrift = false\nlatestReleaseTag = null\n\n// TagStagingPanel.kt:257-259 (dead branches):\nentry.isDrift -> \"\u26a0 Drift\"\nentry.registryStatus == RegistryStatus.VALID -> \"\u2713 OK\"\nentry.registryStatus == RegistryStatus.NOT_FOUND -> \"\u2717 Missing\"",
    "suggestedFix": "Remove `isDrift`, `latestReleaseTag`, and the renderer branches that depend on them from `TagEntry` and `TagStagingPanel`. Simplify `RegistryStatus` to just `UNKNOWN` and `ERROR` (keep ERROR for future use), or delete the enum entirely if only UNKNOWN remains. Remove the `DriftResult.latestReleaseTag` field if `DriftResult` itself is not deleted via CLE-3.",
    "verdictReasoning": "Confirmed by reading TagBuilderService.kt (lines 218-221, 245-248), BaselineCacheService.kt (lines 198-201), TagStagingPanel.kt (line 357-360): every production assignment sets isDrift=false, registryStatus=RegistryStatus.UNKNOWN, latestReleaseTag=null. The renderer branches at TagStagingPanel.kt lines 257-259 (isDrift -> '\u26a0 Drift', VALID -> '\u2713 OK', NOT_FOUND -> '\u2717 Missing') and lines 308-324 (color branches for VALID/NOT_FOUND/isDrift) can never fire at runtime. RegistryStatus.VALID, NOT_FOUND, and CHECKING are dead enum values. DriftDetectorService.isRegistryConfigured() always returns false (CLAUDE.md: 'no-op after registry calls removed'). Removing these fields and renderer branches is safe cleanup with no risk of breaking functionality, since no path sets them to non-default values.",
    "recommendedAction": "Safe to remove isDrift, latestReleaseTag from TagEntry and the corresponding renderer branches in TagStagingPanel. Simplify RegistryStatus to UNKNOWN and ERROR (keep ERROR for setValueAt's user-edit reset at TagStagingPanel line 274). Update AutomationModelsTest which constructs TagEntry with isDrift=true and latestReleaseTag='2.4.0' in the first test.",
    "panel": null
  },
  {
    "id": "AUTOMATION-CLE-5",
    "module": "automation",
    "tab": "Automation",
    "lens": "cleanup",
    "category": "cleanup",
    "severity": "Low",
    "title": "loadBaseline() is a legacy delegation stub with no production callers",
    "file": "automation/src/main/kotlin/com/workflow/orchestrator/automation/service/TagBuilderService.kt",
    "description": "The `loadBaseline()` method at line 254 is marked `/** Legacy method \u2014 delegates to [loadBaselineWithDiagnostics]. */` and has zero production call sites (confirmed by `rg` across the full worktree). Its only callers are three test assertions in `TagBuilderServiceTest` that test the delegation rather than the underlying logic.",
    "evidence": "/** Legacy method \u2014 delegates to [loadBaselineWithDiagnostics]. */\nsuspend fun loadBaseline(suitePlanKey: String): List<TagEntry> =\n    loadBaselineWithDiagnostics(suitePlanKey).tags\n// Zero production callers; only called from TagBuilderServiceTest lines 68, 156, 169",
    "suggestedFix": "Delete `loadBaseline()`. Update the three test call sites in `TagBuilderServiceTest` to call `loadBaselineWithDiagnostics(...)?.tags` directly so the tests continue exercising the real code path.",
    "verdictReasoning": "Confirmed by searching the entire worktree: loadBaseline() at TagBuilderService.kt line 254 has zero production callers. Only TagBuilderServiceTest.kt lines 68, 156, and 169 call it. The method is marked '/** Legacy method \u2014 delegates to [loadBaselineWithDiagnostics]. */' and purely delegates to loadBaselineWithDiagnostics(...).tags. Removing it requires only updating the three test call sites to call loadBaselineWithDiagnostics(...).tags directly. The suggested fix does not risk losing any logic or backward-compat surface since this is a non-public service method.",
    "recommendedAction": "Delete loadBaseline() and update the three test call sites to call loadBaselineWithDiagnostics(...)?.tags (null-safe, since loadBaselineWithDiagnostics always returns non-null). Low priority.",
    "panel": null
  },
  {
    "id": "AUTOMATION-CLE-6",
    "module": "automation",
    "tab": "Automation",
    "lens": "cleanup",
    "category": "cleanup",
    "severity": "Low",
    "title": "singlePageMode branch in scoreAndRankRuns is redundant dead complexity",
    "file": "automation/src/main/kotlin/com/workflow/orchestrator/automation/service/TagBuilderService.kt",
    "description": "The `singlePageMode` check at lines 90\u201399 introduces a conditional that always resolves to the same Bamboo API call. When `singlePageMode = true` (i.e., `targetParseable == maxWalk`), the true-branch calls `getRecentBuilds(suitePlanKey, targetParseable)` and the false-branch calls `getRecentBuilds(suitePlanKey, maxWalk)` \u2014 but at that point `targetParseable == maxWalk`, so both calls are identical. The `if/else` can be collapsed to a single unconditional call `getRecentBuilds(suitePlanKey, maxWalk)`.",
    "evidence": "val singlePageMode = targetParseable == maxWalk\nval buildsResult = if (singlePageMode) {\n    bambooService.getRecentBuilds(suitePlanKey, targetParseable)  // same as maxWalk\n} else {\n    bambooService.getRecentBuilds(suitePlanKey, maxWalk)\n}",
    "suggestedFix": "Remove the `singlePageMode` variable and the `if/else` block. Replace with a single call: `val buildsResult = bambooService.getRecentBuilds(suitePlanKey, maxWalk)`. Remove the corresponding KDoc paragraph explaining the distinction, which is also no longer accurate.",
    "verdictReasoning": "Confirmed by reading TagBuilderService.kt lines 90-99. The condition val singlePageMode = targetParseable == maxWalk guards a branch that calls getRecentBuilds(suitePlanKey, targetParseable) vs getRecentBuilds(suitePlanKey, maxWalk). When singlePageMode=true, targetParseable == maxWalk, so both arguments are identical \u2014 the if/else is a no-op distinction. The real pagination difference is in the early-exit logic at line 179 (if ranked.size >= targetParseable break), not in the fetch call. Collapsing to a single unconditional getRecentBuilds(suitePlanKey, maxWalk) is safe. The KDoc explaining the legacy single-page mode as a distinct fetch is misleading and should be simplified.",
    "recommendedAction": "Remove the singlePageMode variable and the if/else block, replacing with a single call: val buildsResult = bambooService.getRecentBuilds(suitePlanKey, maxWalk). Update the KDoc paragraph that incorrectly explains the if/else as a meaningful distinction. No functional change.",
    "panel": null
  },
  {
    "id": "AUTOMATION-CLE-8",
    "module": "automation",
    "tab": "Automation",
    "lens": "cleanup",
    "category": "cleanup",
    "severity": "Low",
    "title": "CurrentRepoContext.branchName and .detectedFrom are struct fields never read in production",
    "file": "automation/src/main/kotlin/com/workflow/orchestrator/automation/model/AutomationModels.kt",
    "description": "`CurrentRepoContext` carries two fields \u2014 `branchName: String` (line 104) and `detectedFrom: DetectionSource` (line 106) \u2014 that are never read by any production consumer. `TagBuilderService.replaceCurrentRepoTag()` only accesses `context.serviceName` and `context.featureBranchTag`. All three call sites in `AutomationPanel` pass `focusBuild.branch` and `DetectionSource.SETTINGS_MAPPING` but these values go nowhere. The `DetectionSource` enum's values `PROJECT_NAME` and `GIT_BRANCH` are never used in production (only `SETTINGS_MAPPING` is passed, and even that is dropped).",
    "evidence": "// TagBuilderService.kt:257-274 \u2014 replaceCurrentRepoTag only reads serviceName and featureBranchTag:\nfun replaceCurrentRepoTag(entries: List<TagEntry>, context: CurrentRepoContext): List<TagEntry> {\n    if (context.featureBranchTag == null) return entries\n    return entries.map { entry ->\n        if (entry.serviceName == context.serviceName) { ... } else { entry }\n    }\n}\n// context.branchName and context.detectedFrom never accessed in production code",
    "suggestedFix": "Remove `branchName` and `detectedFrom` from `CurrentRepoContext`. Simplify the data class to `data class CurrentRepoContext(val serviceName: String, val featureBranchTag: String?)`. Remove `DetectionSource` enum entirely. Update the three `AutomationPanel` call sites and the two test cases that construct `CurrentRepoContext` with 4 arguments.",
    "verdictReasoning": "Confirmed by reading TagBuilderService.kt lines 257-274: replaceCurrentRepoTag only accesses context.serviceName and context.featureBranchTag. The branchName and detectedFrom fields are never read in any production code path. All three AutomationPanel call sites (lines 561-565, 644-648, 816-820) pass focusBuild.branch and DetectionSource.SETTINGS_MAPPING but these values are carried by the struct and never consumed. DetectionSource.PROJECT_NAME and GIT_BRANCH are never used as values anywhere in production (only SETTINGS_MAPPING is passed, itself ignored). AutomationModelsTest line 108-116 tests CurrentRepoContext with all 4 args. Removing branchName and detectedFrom is safe \u2014 no consumer reads them.",
    "recommendedAction": "Remove branchName and detectedFrom from CurrentRepoContext data class. Remove DetectionSource enum entirely. Update the three AutomationPanel call sites (remove the two dead args) and update AutomationModelsTest to drop the DetectionSource field. Low priority cleanup.",
    "panel": null
  },
  {
    "id": "AUTOMATION-ARC-4",
    "module": "automation",
    "tab": "Automation",
    "lens": "architecture",
    "category": "architecture",
    "severity": "Low",
    "title": "UI panels own manually-allocated CoroutineScope instead of using platform-injected scope",
    "file": "automation/src/main/kotlin/com/workflow/orchestrator/automation/ui/AutomationPanel.kt",
    "description": "Four UI classes \u2014 `AutomationPanel` (line 57), `MonitorPanel` (line 53), `QueueStatusPanel` (line 44), and `AutomationStatusBarWidgetFactory`'s inner widget (line 33) \u2014 each allocate their own `CoroutineScope(Dispatchers.IO + SupervisorJob())`. The `:core` CLAUDE.md and phase-4 conventions explicitly deprecate this pattern for `@Service` classes in favour of the platform-injected `cs: CoroutineScope`. UI panels are not `@Service` instances, so the impact is lower, but creating four independent `SupervisorJob` roots means any unhandled exception in one coroutine is silently swallowed by the Job (the `launchWithErrorSurface` wrapper in `QueueService` is absent in the panel scopes), and cancellation ordering is ad-hoc (`scope.cancel()` in `dispose()`). A leak in `dispose()` not being called (e.g. if `Disposer.register` was omitted for a child panel) would permanently prevent GC of the scope's coroutines.",
    "evidence": "// AutomationPanel.kt:57\nprivate val scope = CoroutineScope(Dispatchers.IO + SupervisorJob())\n// MonitorPanel.kt:53\nprivate val scope = CoroutineScope(Dispatchers.IO + SupervisorJob())\n// QueueStatusPanel.kt:44\nprivate val scope = CoroutineScope(Dispatchers.IO + SupervisorJob())",
    "suggestedFix": "For non-`@Service` Disposable UI panels the accepted IntelliJ 2024.1+ pattern is `cs.childScope()` or to obtain the project's `@Service`-injected scope via a thin `@Service`-level bridge (e.g. have a `@Service` own the scope and let the panel call `cs.launch { }`). At minimum, check that every panel that allocates its own scope has a matching `Disposer.register` so `dispose()` is always called, and add a top-level `CoroutineExceptionHandler` so exceptions in fire-and-forget coroutines surface in logs.",
    "verdictReasoning": "Confirmed: AutomationPanel.kt:57, MonitorPanel.kt:53, QueueStatusPanel.kt:44, and AutomationStatusBarWidgetFactory.kt:33 each allocate their own CoroutineScope(Dispatchers.IO + SupervisorJob()). However, the finding overstates the leak risk. All panels properly call scope.cancel() in their dispose() methods (AutomationPanel:1166, MonitorPanel:773, QueueStatusPanel:170, status bar widget:85). AutomationPanel registers all child panels via Disposer.register at lines 356-359, and the tool-window factory calls content.setDisposer(panel) (WorkflowToolWindowFactory.kt:325-326, 346, 367) so the top-level dispose cascade is wired. The AutomationStatusBarWidgetFactory inner widget extends EditorBasedWidget which is Disposable managed by the platform. No actual lifecycle leak exists in the current code. The gap is a style/convention issue (not following the platform-injected cs pattern for @Service classes per core CLAUDE.md) but panels are not @Service classes \u2014 the convention docs explicitly say 'Non-@Service classes consolidate fire-and-forget launches onto a single field scope.' Severity remains Low.",
    "recommendedAction": "Add a CoroutineExceptionHandler to each panel scope to surface unhandled exceptions in logs rather than swallowing them silently. No structural change required for lifecycle correctness \u2014 all dispose() paths are already wired.",
    "panel": null
  },
  {
    "id": "AUTOMATION-ARC-6",
    "module": "automation",
    "tab": "Automation",
    "lens": "architecture",
    "category": "architecture",
    "severity": "Low",
    "title": "TagBuilderService @Service without platform-injected CoroutineScope takes Project but launches its own suspend work without a scope",
    "file": "automation/src/main/kotlin/com/workflow/orchestrator/automation/service/TagBuilderService.kt",
    "description": "TagBuilderService is annotated `@Service(Service.Level.PROJECT)` but does not follow the platform-injected `cs: CoroutineScope` pattern mandated by the phase-4 conventions. Its `suspend` entry points (`scoreAndRankRuns`, `detectDockerTag`, `loadBaseline`) are called from caller-owned scopes (panel scopes), which is acceptable, but the IntelliJ DI constructor reads `PluginSettings` and `BuildLogCache` eagerly at construction time without deferring to lazy \u2014 in a headless test environment this will throw because `PluginSettings.getInstance(project)` invokes service resolution on the DI container before the plugin's state is initialised. The test constructor (the second one) is the workaround, but it means every test must remember to use the test constructor rather than `project.getService()`.",
    "evidence": "constructor(project: Project) {\n    val settings = PluginSettings.getInstance(project)\n    this.bambooService = project.getService(BambooService::class.java)\n    this.buildLogCache = BuildLogCache.getInstance(project)\n    this.buildVariableName = settings.state.bambooBuildVariableName?.takeIf { it.isNotBlank() } ?: \"DockerTagsAsJSON\"\n}",
    "suggestedFix": "Lazy-init `buildVariableName` by reading it at call time from `PluginSettings` rather than caching it at construction, so there is no settings read in the constructor. For `bambooService` and `buildLogCache`, the platform DI constructor already receives `Project` \u2014 use lazy delegates (`by lazy { project.getService(...) }`) consistent with the pattern used in `AutomationPanel` and `QueueService`. This removes the eager-resolution fragility without requiring a separate test constructor.",
    "verdictReasoning": "Confirmed: TagBuilderService project constructor (line 29-35) reads PluginSettings.getInstance(project), project.getService(BambooService::class.java), and BuildLogCache.getInstance(project) eagerly at construction time. This is a convention gap relative to the platform-injected cs pattern and the lazy-delegate idiom used by QueueService. In headless tests, tests correctly use the second constructor (test constructor at lines 37-46) to avoid DI issues. The bambooService and buildVariableName fields are not lazy. In the test file, service = TagBuilderService(bambooService) is used directly. Practically the failure mode is contained by the test constructor seam. The finding is real as a maintainability/convention issue but not a runtime bug for production use since the DI context is always available when the platform constructs the service. Severity remains Low.",
    "recommendedAction": "Apply lazy delegates for bambooService and buildLogCache; read buildVariableName at call time from PluginSettings to remove eager-construction fragility and align with the QueueService pattern. Remove the test constructor once lazy init is in place.",
    "panel": null
  }
]