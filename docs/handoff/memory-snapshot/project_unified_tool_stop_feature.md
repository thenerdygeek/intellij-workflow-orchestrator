---
name: unified-tool-stop-feature
description: Unified per-tool Stop button + graceful teardown for all agent tools — PR
metadata: 
  node_type: memory
  type: project
  originSessionId: 958eb394-c2cf-4b41-84e5-485ee506bc52
---

**2026-06-20: Unified tool-stop feature — ✅ MERGED to main via PR #62 squash `cf3d22873` (2026-06-20T18:12Z).** Decided safe-to-merge WITHOUT in-IDE smokes: additive (only run_command was stoppable before) + graceful degradation (worst miss = "Stop abandons result, work finishes bg" = already the un-migrated-PSI-tools state; isUserStop precise + ensureActive re-throw safety net) → no data-loss/crash/regression path; a smoke miss = localized checkCanceled follow-up, not a revert. **In-IDE smokes STILL pending post-merge** — blocked: needs live IDE + Sourcegraph token (agent brain IS Sourcegraph; no token → no tool call originates → nothing to Stop; ToolTestingPanel bypasses the funnel so can't test the button). runIde launches IDEA **Ultimate** 2025.1.7 (db plugin bundled → db_query smoke works). Smoke runbook committed: `docs/superpowers/specs/2026-06-20-unified-tool-stop-smoke-tests.md` (3 affordances + trigger prompts + ps/top/pg_stat_activity observation). Jira token NOT needed. Worktree `.claude/worktrees/feature+unified-tool-stop` + backup branch `backup/unified-tool-stop-pre-rebase` PRESERVED (for the deferred smokes). Per-tool-call Stop button for *every* agent tool (was only run_command); stopping aborts only that tool, returns "[Stopped by user]", loop continues. Plus graceful teardown on every cancel path. **REBASED onto `origin/main` #64 (`903636c9f`) 2026-06-20** — branch now `worktree-feature+unified-tool-stop` @ `d4532da6a`, **0 behind / 25 ahead**, clean. (Was @ `9ba9a48b9` on 61ddd4e21.) Worktree `.claude/worktrees/feature+unified-tool-stop` preserved; backup branch `backup/unified-tool-stop-pre-rebase`. Full handoff: `docs/superpowers/specs/2026-06-20-unified-tool-stop-handoff.md` (+ design spec same dir; SDD ledger `.superpowers/sdd/progress.md` gitignored).

**Rebase onto #64 (the conflict was SEMANTIC):** #64 = "make :agent detekt green (fix genuine findings + baseline formatting)" — it FIXED the #61 detekt debt that this branch had previously *baselined* (in the now-dropped `8a75cbd59`/`328b73168`). So resolved all `detekt-baseline.xml` conflicts to main's side (`--ours`); the pure-baseline commit `328b73168` became empty → git dropped it (25→24, then +1 reconcile commit = 25). The merged `AgentLoop.kt` (UnifiedMessageQueue from #61 + cancel.*/coroutineScope funnel imports) + `AgentController.kt` (+ToolStopCoordinator) shifted their import-block strings → 2 resurfaced ImportOrdering findings → surgically updated exactly those 2 baseline entries (commit `d4532da6a`). AgentLoop.kt funnel (~L2055) and #61 queue drain (~L940/2670) are in non-overlapping regions; auto-merge was clean + verified coherent. **Re-verified GREEN on top of #64:** :agent:detekt + :web:detekt; :agent:test + :web:test (clean, `--no-build-cache`, 0 fail, no NoSuchMethodError, no flake); vitest 772/772. PR #62 was CONFLICTING pre-rebase.

**Mechanism:** `tools/cancel/` = ToolCancellationRegistry (toolCallId→Job) + ToolStopCoordinator (ProcessRegistry.kill → else coroutine cancel) + isUserStop/stoppedByUserResult. Funnel: `AgentLoop.executeToolCalls` wraps each tool.execute in `coroutineScope`+register/unregister; on UserStopCancellationException → return stoppedByUserResult (loop continues), else re-throw. `_killToolCall` bridge rewired to the coordinator. Universal Stop button in `ToolCallChain.tsx`, `STOP_SUPPRESSED_TOOLS={run_command,background_process,ask_user_input,agent}`. Sub-agents stop via the pre-existing per-worker Kill (cancelAgent→abort). Blocking tools made to actually abort: smartReadAction (find_references/definition), ProgressManager.checkCanceled in visitor walks (run_inspections/diagnostics + JavaKotlin/Python providers), Statement.cancel (db_query), OkHttp Call.cancel (web_fetch + SearXNG/CustomHttp search providers).

**Built via subagent-driven dev** (TDD + per-task + per-phase + final reviews). Reviews caught: virtual-clock-flaky funnel test, detekt import-baseline trap, 6 debt-hiding suppressions, and a **Critical** run_inspections PCE-swallow (inner `catch(Exception)` ate the ProcessCanceledException → Stop was dead while the source-text contract passed green).

**Verified:** automated green on top of main (compile + tool-stop tests + detekt + vitest 772) — **must use `--no-build-cache`** (#61 changed AgentLoop ctor → stale-cache NoSuchMethodError trap on cached runs). **In-IDE smokes still pending** (the only proof Stop actually *bites* on blocking tools — source-text contracts prove presence not bite).

**Open items:** (1) ✅ RESOLVED by the rebase onto #64 — #64 already fixed #61's ~130 detekt findings on main, so this branch's old baseline workaround is gone. (2) ParallelSubagentIntegrationTest ThreadLeakTracker flake (pre-existing, unrelated — did NOT fire in the post-rebase run). (3) ShortenerResolver left uncancelled (sub-second probe). (4) ~12 other PSI tools still executeSynchronously (button shows but only abandons result) = possible Phase 3.5. (5) ✅ Force-pushed `d4532da6a` (`--force-with-lease`) 2026-06-20 → PR #62 now MERGEABLE/CLEAN (was CONFLICTING/DIRTY). (6) In-IDE smokes still pending (the only proof Stop *bites*).

**Gotchas:** detekt import-block baseline trap (surgical-update the one entry, never bulk-regen except post-rebase reconcile; never baseline new code); PCE/CancellationException swallow by broad catch (use dedicated typed catch before the broad one, NOT `if (e is)` which trips detekt InstanceOfCheckForException); `webview/dist` gitignored + gradle-built (don't commit). User feedback this session: use sonnet/opus for subagents, never haiku ([[subagent-model-no-haiku]]).
