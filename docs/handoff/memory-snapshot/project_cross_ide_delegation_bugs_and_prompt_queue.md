---
name: project_cross_ide_delegation_bugs_and_prompt_queue
description: Cross-IDE delegation — 4 root-caused bugs + round-2 6 bugs ALL FIXED + shipped (.19/.20); 2026-06-01 .18-feedback re-confirmed fixed + close→CANCELED cosmetic also fixed
metadata: 
  node_type: memory
  type: project
  originSessionId: e8a0b8b7-1e5d-401d-9925-fe59e2fb53ba
---

Branch `feature/cross-ide-delegation`. **SHIPPED: original campaign = release .19 (user); round-2 6-bug fixes = release v0.85.47-alpha-cross-ide.20 (pushed `2d6a24cdf`, prerelease, ZIP attached, 2026-05-30; manual two-window smoke PASSED).** Remaining open: queued prompt-staleness was folded into .19's Fix 6 (done); routeQuestion per-question timeout still QUEUED (needs setting+UI). Below: original diagnosis + both campaigns' detail.

**QUEUED (not started) — system-prompt staleness.** The `delegation` tool SCHEMA is current (7 actions: `DelegationTool.kt:134` enum + `description`), but the secondary guidance surfaces still describe the OLD 5-action design, missing `status`/`wait` (added 2026-05-30): SystemPrompt Section-5 hints (`SystemPrompt.kt:579-583`), `cross-ide-delegation` SKILL.md frontmatter, and `tool-docs/delegation.md` ("Five verbs"). Fix = add status/wait + the attach-vs-don't-poll flow to those 3 surfaces; extend `SystemPromptDelegationGateTest` (only asserts send/fetch_transcript today). System prompt IS correctly gated+rebuilt on `enableOutboundCrossIdeDelegation` (`AgentService.kt:2064,2883`).

**DIAGNOSED 2026-05-30 — 4 delegation runtime bugs.** Common cause: the 2026-05-30 retention/wait rework (commits `6845ce6e3`, `12cf38e65`) half-wired the new "handle outlives channel" + repeated-delegation lifecycle.

- **A. Handle-state split (High).** `status`(`DelegationOutboundService.kt:1052`)+`fetch_transcript`(`:993`) read `retainedHandles`; but `sendContinuation`(`:611` reads `handleToSessionId`)+`answer`→`hasOpenChannel`(`:972` reads `activeChannels`) read only live maps `close()` wipes → same handle: status=closed, send=handle_not_found, answer=HandleNotFound. Also `handleToLastSeenState` seeded "RUNNING"(`:226`), never updated on terminal Result → misleading `last_state:RUNNING` for completed. Reattach infra EXISTS (core `ChannelResume`/`UserTurn` + `RetainedHandle.bSessionId`/`targetProjectPath`) but unused post-completion; IDE-B `handleChannelResume` returns `SessionClosed` for terminal sessions (no resurrection) — true continuation needs an IDE-B-side resume-of-persisted-session.
- **B. Works-once / 2nd delegation times out (High).** IDE-B `AgentController.currentSessionId`/`sessionActive` set on delegated start (`AgentController.kt:3211-3212`), NEVER cleared on completion (only `resetForNewChat` `:3260-3261`; delegated path wires no `onComplete`). 2nd delegation → busy(`:3090`)→QUEUE_INCOMING→55s no-Start-click→`DECLINED_TIMEOUT`(`DelegationInboundService.kt:319`). IDE-A side CLEAN. `declined_timeout` is a bare token (actually = IDE-B human didn't click Start in 55s). Secondary (Allow-once, within 10s): `lastDialogAt` never cleared (`DelegationDoorbellService.kt:210`) + IDE-A ignores `KnockAck.DUPLICATE` (`DelegationOutboundService.kt:344-353`) → 90s dead poll.
- **C. Picker offline-on-first (Medium).** Picker single-probes work socket (`DelegationPicker.kt:455`), 3-state, no "available"; `list_targets` dual-probes + `resolveTargetStatus` (`DelegationTool.kt:939-948,978-981`). Port doorbell-aware dual-probe to picker (+ add AVAILABLE status/renderer). Not a cache/timeout race.
- **D. Cold-launch consent dialog never shows (High for closed-IDE).** `knockAndWaitForBind` `finally{store.clear(nonce)}` (`DelegationOutboundService.kt:378-381`) deletes pending file at end of 90s `CONSENT_WAIT_TIMEOUT`; cold IDE indexes > 90s → file gone before `replayPendingRequests` (`runWhenSmart`, post-index) reads it. Secondary: `REPLAY_TTL_MS=5min` (`DelegationDoorbellService.kt:335`) < large-repo index. Path-derivation/registration/dedupe all VERIFIED OK.

**Campaign progress (uncommitted working tree, branch `feature/cross-ide-delegation`).** User chose FULL fix incl. true continuation. Plan: `docs/superpowers/plans/2026-05-30-cross-ide-delegation-bugfix-campaign.md`. Order B→A→continuation→D→C→prompt/docs. TDD, sequential (shared files), subagent-driven, foreground.
- **Fix B (works-once) — LANDED + verified.** Gate on live job not loaded session: `AgentController.decideIncomingBusy` (~102, used ~3113); `declined_timeout` now specific (DelegationInboundService ~324); doorbell `lastDialogAt` cleared via `clearDedupeSlots` (~256); IDE-A honors `KnockAck.DUPLICATE` (DOS ~344). Tests: IncomingDelegationBusyGateTest + BusyDelegationTopBarTest + DelegationDoorbellServiceTest.
- **Fix A (handle consistency) — LANDED + verified.** SSOT `handleState(handleId): HandleState{Active/ClosedRetained/Unknown}` (DOS ~1112) routes status+answer+sendContinuation so all agree; `recordTerminalState` (~519, called in Result branch + socket-EOF FAILED) fixes the stale `last_state:RUNNING`. Tests: DelegationHandleConsistencyTest + DelegationActionConsistencyTest.
- **Step 3 (true continuation) — LANDED + verified.** IDE-B `handleChannelResume` terminal branch reads the follow-up UserTurn → NEW `AgentService.resumeDelegatedSession` (wraps `resumeSession(sessionId,userText=…)`; AgentService.kt:2608/2687 already continues a completed session) + `AgentController.resumeDelegatedSession` (busy-gate + banner, skips consent). IDE-A `sendContinuation` ClosedRetained → `resurrectAndContinue` dials from `retainedHandles`, ChannelResume+UserTurn, re-registers channel + routes result to CURRENT delegator. `DelegationTrueContinuationE2ETest` (5). ⚠ impl connection dropped mid-run leaving one undefined method (`resurrectAndContinue`, DOS.kt:660, only compile error); resumed agent `aeb5b696a5f3973b5` finished it.
- **Fix D (cold-launch) — LANDED + verified.** Removed unconditional `finally{store.clear}`; clear only on success-bind + decline; cold-launch timeout LEAVES the file for post-index replay. `REPLAY_TTL_MS` 5→30min. `applyConsent` clears consumed file via new `clearPending` (preserves `.declined`). ⚠ const-val build-cache trap (REPLAY_TTL inlines → final verify needs --no-build-cache).
- **Fix C (picker) — LANDED + verified.** New shared `TargetStatusResolver` (pure resolve + injectable dualProbe); picker + list_targets both wired through it; `AVAILABLE` status (amber, selectable).
- **Fix 6 (prompt/docs) — LANDED + verified; CLEARS the parity red.** DelegationTool DSL gains status/wait actions + timeout_seconds + handle desc matched to schema (fixes ToolDslSchemaParityTest); SystemPrompt Section-5 status/wait/attach hints; SKILL.md + tool-docs/delegation.md rewritten for 7 actions + continuation-after-completion + 30min retention + new error taxonomy; SystemPromptDelegationGateTest extended (anti-drift). Snapshots byte-stable (gated off by default).
- **CAMPAIGN COMPLETE — full clean `:agent:test` (--no-build-cache --rerun-tasks) BUILD SUCCESSFUL (4401 tests, 0 fail, 9 skip).** 25 files (10 main + TargetStatusResolver + SystemPrompt + SKILL + tool-docs + 11 tests). NOT committed; NOT released. ⚠ **Manual cross-IDE smoke STILL PENDING** (two real IDE windows: repeat delegation, continuation-after-completion, cold-launch+index, inbound-off picker AVAILABLE) — automated tests can't drive live sockets/consent UX. Minor queued follow-ups: already-running-inbound-off pending file lingers ≤30min (Fix D risk); `defaultRecentsProvider` probe→status→string round-trip polish (Fix C); `hasOpenChannel` now test-only (Fix A).
- **Workflow `cross-ide-delegation-bughunt` (user request) — RAN (resumed wf_87aaa01a-f53 after a session-limit interrupt). 6 confirmed bugs in 3/12 scenarios; 9 scenarios clean (incl. Fixes A/C/D areas handle-consistency/picker/cold-launch — corroborates the campaign).** ⚠ NOT yet fixed — awaiting user scope decision. Verifier is non-deterministic (run1 confirmed doorbell/cold-launch; run2 confirmed result-delivery/continuation — these 6 are run2's set, the synthesized one). I independently code-verified #1/#2/#5; #3 is Fix B's self-flagged limitation; #4/#6 agent-reported.
  - **#1 HIGH `wait`/Result race loses the result** — `DelegationOutboundService.kt` awaitResult `:1311-1317` (withTimeoutOrNull cancels await() only, deferred stays in `pendingResultWaiters`) vs reader Result branch `:480-485` (`remove()?.complete()` doesn't check return, skips `onResult` nudge). Fix: reader falls back to onResult when complete()==false, or atomically claim the deferred.
  - **#2 HIGH idle delegation nudge dropped on auto-wake guard reject** — `AgentService.kt` enqueueNudgeForSession `:552` calls autoWakeIdleSession with NO persist, unlike onBackgroundCompletion persistForLaterResume `:437`. Cooldown/cap/disabled/no-listener → result lost; dropped question deadlocks remote (routeQuestion awaits w/o timeout). Fix: persist-before-auto-wake + replay in resume preamble.
  - **#3 HIGH concurrent inbound busy-gate race** — `AgentController.kt` gate `:3113` reads currentJob on socket coroutine; runDelegatedNow assigns it later on EDT `:3209-3255` → two near-simultaneous delegations both RUN_NOW → #2's resetForNewChat cancels #1. (Fix B impl flagged this exact window.) Fix: Mutex/CAS around gate-read→currentJob-assignment.
  - **#4 MED auto-wake hijacks the user's current chat** — `AgentController.kt` wireAutoWakeListener `:649` → resumeSession → prepareForReplay `:3839` unconditionally cancelCurrentTask+reset with no guard requested==active session. Fix: deliver-without-cancel when target≠active.
  - **#5 MED resumed/resurrected channels never install IdleTimer** — `DelegationOutboundService.kt` resurrectAndContinue ~`:878` (+attemptResume, sendContinuation-resumed); only send() `:239-268` creates a timer + readFramed has no read timeout → hung IDE-B leaks channel+reader forever, counts toward MAX_CHANNELS. (MY Step 3 gap.) Fix: install IdleTimer on every resume/resurrect path.
  - **#6 LOW unsynchronized writeFramed (heartbeat vs routeQuestion)** — `core/DelegationServer.kt` replyWith `:150` no write mutex; collision routeQuestion vs HeartbeatScheduler. Narrow (JDK write atomic per-call; needs partial write under back-pressure). Fix: per-channel write Mutex (mirror outbound sendMutex).
- **ROUND 2 COMPLETE + verified (2026-05-31, uncommitted, 16 files).** All 6 workflow bugs fixed, TDD, sequential subagent-driven: R1 #1 wait-race (`deferred.complete(TimedOut)` arbiter) + #5 `installIdleTimer` on resume/resurrect; R2 #2 `persistDelegationNudgeForLaterResume` before wake + replay (new `DelegationNudgePersistence`) + #4 `DEFER_ACTIVE_SESSION` route + EDT TOCTOU re-check; R3 #3 lock-free CAS `DelegationStartReservation` atomic check-and-claim on both start+resume gates; R4 #6 per-connection `writeMutex` in `DelegationServer.replyWith`. Full clean `:core:test`+`:agent:test` BUILD SUCCESSFUL; `verifyPlugin` Compatible. routeQuestion-timeout QUEUED (bounded by idle timer; needs setting+UI).
- **⚠ RELEASE STATE:** USER committed the ORIGINAL campaign as `49da6b657` + released **.19** (`9491e8485`, PUSHED + public GH release; does NOT contain round-2). Round-2 NOW committed by Claude (no trailer, matching user style): `0816ba418` (fix, 16 files) + `2d6a24cdf` (chore(release): **.20**). `clean buildPlugin` green → `build/distributions/intellij-workflow-orchestrator-0.85.47-alpha-cross-ide.20.zip` (65M). Branch **ahead of origin by 2, NOT pushed, NO .20 GH release yet** — HELD for the user's manual two-window smoke (user chose "smoke before releasing .20"). **On user confirm: `git push` + `gh release create v0.85.47-alpha-cross-ide.20` with the .20 ZIP.**
- **⚠ STILL PENDING:** manual two-window cross-IDE smoke (repeat delegation, continuation-after-completion, cold-launch+index, picker AVAILABLE, concurrent inbound delegations) — automated tests can't drive live sockets/consent. `.19` was released without it.

**✅ COMMITTED+PUSHED (NOT RELEASED) 2026-06-01 — `.23`-feedback follow-ups + IDE-B UX**, commit `b0d46bcf1` on `origin/feature/cross-ide-delegation` (branch now 1 ahead of the `.23` tag; `pluginVersion` still `.23` — NO `.24` cut yet). Round-3 feedback (.23 build) + delegated-path UI-callback audit. Full `:core:test`+`:agent:test` green (no-cache rerun) + webview vitest 694. Contents: **(#4 cancel propagation, CRITICAL)** `DelegationMessage.CancelTask` frame — IDE-A close/cancel now actually STOPS IDE-B's agent Job (was orphaned/running-to-completion); EOF-cancel when non-terminal, terminal-reached guard avoids cancelling normal completion. **(.23#5)** `ide_b_busy` reclassified Expired→**Rejected (RETRYABLE, handle stays valid)**; Expired reserved for handle-gone; 2 stale tests (`DelegationLowFindingsTest` Plan-4-scaffolding pin, `DelegationTrueContinuationE2ETest` busy assertion) updated — ⚠ these were missed by the #20 sub-agent's per-slice run and only caught by the authoritative whole-tree no-cache gate (lesson: always run the combined gate before commit). **(.23#3)** `wait` on CANCELED/closed handle reports true terminal (`already_canceled`) not "already completed" (`awaitResult` reads `RetainedHandle.lastState`). **(.23#2)** `list_targets` advisory `running (busy)`/`(idle)` via backward-compat `Pong.busy` (null=old peer). **(.23#7)** `delegationAcceptWindowSeconds` setting [10,59] + UI; busy decline names in-flight task. **(#1 UI)** banner + consent dialog show delegator REPO not `ide-<pid>`. **(#2 UI)** delegated bubbles get tint+accent-stripe+`delegated · <repo>` pill on every bubble (per-message flag, text prefix dropped); **(#3 UI)** picker intent header + task preview + per-target send-explainer. **prompt:** `list_handles` added to SystemPrompt Section 5. .23#1/#4/#6 were resolved transitively by #4 cancel-propagation. ⚠ STILL no two-window manual smoke. Detail of `.23` release itself below:

**✅ SHIPPED 2026-06-01 — release v0.85.47-alpha-cross-ide.23** (commits `af30a5149` work + `8d95f874e` release bump, PUSHED to origin `feature/cross-ide-delegation`, GH prerelease + 65M ZIP). Second feedback round + delegated-path structural audit. Full `:core:test`+`:agent:test` green (no-cache rerun), webview build + vitest 691, verifyPlugin Compatible. Contents: (protocol/behavior) ClosedChannelException result-delivery now best-effort (peer hung up ≠ FAILED; `DelegationServer` reply writes tolerate ClosedChannelException/broken-pipe, FrameSizeExceeded still propagates); **C** nested `session_closed: ide_b_busy:` fixed (drop prefix when closeReason carries a taxonomy token; `classifyResumeCloseReason`); **F** new `delegation(action="list_handles")` (`handlesForSession` + `HandleSummary`) + self-describing busy decline (`composeBusyDeclineReason` names in-flight task + delegator session id; subsumes B wording); **G** `status` reports `retention_expires_in_seconds` (injectable clock, backward-compat for `capturedAt==0`); **E/H/J/D** send selection narration + `already_closed:true` + status/range docs. (IDE-B UI) **Bug 1** `onSubagentProgress` threaded through delegated start/resume → subagent cards render; **Bug 2** delegation conversation cards (`UiSay.DELEGATION_CARD` + `DelegationCardData`; `DelegationQuestion/Answer/ResultCard.tsx`; pushes from `routeQuestion`/`deliverAnswer`/wrapped `onResult`; persisted; Q↔A correlated by `questionId`). (Structural) **`SessionUiCallbacks` bundle + `AgentController.buildSessionUiCallbacks()` SSOT** used by BOTH `executeTaskInternal` and `runDelegated*Now`; delegated `AgentService` seams forward the bundle → closes completion-finalize/token/stats/streaming-edit/retry/compaction/model-switch drop class; delegated = ACT-ONLY (plan tools filtered, `isDelegatedSession`); `SessionUiCallbacksParityTest` locks it. **Audit cleared: approval gate + all hooks + checkpoints + network recovery + compaction are SHARED via executeTask/resumeSession → NO behavior/security gap; the delegated path is NOT a parallel loop.** USER_PROMPT_SUBMIT + local-steering documented as intentionally skipped. (Naming) all user-facing delegation text uses repo names not IDE-A/IDE-B — see [[feedback_delegation_use_repo_names]]; machine tokens (`ide_b_busy`…) + dev logs untouched. ⚠ Two-window manual smoke STILL PENDING (subagent visibility, conversation cards, busy correlation, continuation) — automated tests can't drive live sockets/consent.

**✅ SHIPPED 2026-06-01 — release v0.85.47-alpha-cross-ide.22** (commit `757214e39`, PUSHED to origin `feature/cross-ide-delegation`, GH prerelease created w/ 65M ZIP). All 6 re-verified issues fixed TDD (fail-before/pass-after), full `:core:test`+`:agent:test` green on `--no-build-cache --rerun-tasks`, `verifyPlugin` Compatible. Fixes: **#1** doorbell now replies Ping→Pong (side-effect-free liveness; `DelegationDoorbellService`) so open inbound-OFF IDE resolves `available` (real-socket regression test `DelegationDoorbellPingSocketTest`); **#2b** transient ("Allow once") work socket now stays bound through ~30min retention window (`DelegationInboundService` scheduled unbind, cancelled by new activity/project-close/inbound-off; fresh non-resume Connect still consent-gated — security preserved because protocol already separates Connect-consent from ChannelResume-resume) → continuation-after-completion works; **#2a** docs add "IDE-B PROCESS must stay OPEN" precondition (SKILL.md/delegation.md/SystemPrompt); **#3** fetch_transcript+send-continuation routed so unknown handle = `DelegationHandleNotFound` everywhere (Expired reserved for known-handle reattach/IO); doc overclaim fixed; **#4b** `close()` authors terminal CANCELED into retained snapshot (race-free, `alreadyTerminal` guard preserves natural-completion); **#5** wait echoes clamped timeout_seconds; **#4c** close warns it aborted RUNNING work. **#6 was a non-issue** (wait already documented as the non-poll path). ⚠ STILL PENDING: two-window manual smoke (live sockets/consent can't be automated) — notably #1 AVAILABLE + #2b Allow-once continuation; shipped per user request without it (same as .19/.20). Files: 5 main (DelegationDoorbellService/DelegationInboundService/DelegationOutboundService/DelegationTool/SystemPrompt) + 2 docs + 11 tests (3 new). Diagnosis correction that drove this campaign below:

**⚠ 2026-06-01 RE-VERIFICATION CORRECTION — the ".18 cross-check below was OVER-OPTIMISTIC.** A fresh 6-agent code-grounded re-verify at HEAD `50d9c0f3f` (.21) of the SAME feedback (re-reported 2026-06-01 05:40) DISPROVED my "already fixed" claims for items 1/2/4. Actual state: **#1 list_targets = STILL BROKEN** — dual-probe asks `doorbellReachable` by sending a `Ping`, but doorbell (`DelegationDoorbellService.kt:154,167`) answers ONLY `Knock` and drops else → `ping` always null → `available` branch (`TargetStatusResolver.kt:57-80`) is DEAD CODE → open inbound-OFF IDE (default) always "closed". Tests masked it (`TargetStatusResolverTest.kt:107-122` fakes pingFn by `.doorbell.sock` suffix). **#2 continuation = doc bug (7 spots: SKILL.md:40/66/67, delegation.md:129/136-139/253, SystemPrompt.kt:580/587 omit "IDE-B PROCESS must still be running") + REAL bug on the transient "Allow once" path** — `stopIfTransientAndIdle` (`DelegationInboundService.kt:212`) fired from `AgentService.kt:3284`(+3245/3358/3368/3407) UNBINDS the work socket on completion while IDE-B stays open → continuation re-dial (`resurrectAndContinue` ping `DelegationOutboundService.kt:911-912`) → `ide_b_not_running`. Persistent-inbound-ON is fine. **#3 error-type inconsistency = REAL** — `fetchTranscript` (`DOS.kt:1271-1281`) + send-continuation emit `DelegationExpired: handle_not_found`; status/wait/answer emit `DelegationHandleNotFound: <handle>`. fetch_transcript NOT routed through `handleState` SSOT; `delegation.md:248-258` OVERCLAIMS "all three agree / uniformly not found everywhere" (contradicts own table :286-288 + schema :417); SKILL.md:67 accurate. **#4b status RUNNING after close = STILL REACHABLE (fix incomplete)** — `close()` snapshots retained handle at `DOS.kt:617-623` while reader still blocked (captures RUNNING); reader CANCELED write `:553` lands in already-removed `handleToLastSeenState`, never copied to `retainedHandles`; no test covers close-on-running ordering. **#4a ipc_read_failed:null = genuinely FIXED `34bc281d6` (tester on old build for that symptom). #4c close no-abort-warning = present (bare {"closed":true} DelegationTool.kt:751-776). #5 timeout_seconds silent clamp = CONFIRMED** (`handleWait` coerceIn(5,1800) DelegationTool.kt:892-893, default 300, no requested-vs-effective echo). **#6 do-NOT-poll = NON-ISSUE** (all 3 surfaces clearly frame `wait` as the alternative). LESSON: my .18 cross-check trusted campaign DESIGN INTENT instead of verifying the probe PROTOCOL + the transient consent path — the same "wired plumbing, untested protocol" trap. NOT yet fixed; awaiting user scope decision. — older over-optimistic note retained below for context:

**2026-06-01 — .18 user-feedback cross-check (agent reported 6 delegation issues on the .18 build).** Investigated each vs current HEAD: **Items 1 (list_targets status), 2 (continuation/resurrection), 4 (ide_b_not_running taxonomy) = already FIXED** by the .19/.20 campaigns above (dual-probe `TargetStatusResolver`, `resurrectAndContinue`+`handleChannelResume`, accurate error subcategories). **Item 3 (close→`ipc_read_failed: null` + status stuck RUNNING): RUNNING-stuck already fixed; the cosmetic FAILED-vs-CANCELED + null message FIXED 2026-06-01 (commit `34bc281d6`)** — `DelegationOutboundService` now marks `intentionallyClosing` in `close()`/`cancelAllForSession` so the reader records terminal CANCELED and suppresses the spurious nudge; genuine read errors map null→"channel closed". **Item 5 (`answer` untestable in isolation) = DX/docs gap** (no dry-run; document wait→answer runbook). **Item 6 (target `DelegationTargetNotReachable` after close) = OPEN/uncertain — `close()` has no effect on IDE-B's socket; most likely expected transient (Allow-once) re-consent on next send; NEEDS a two-window manual repro** (inbound-OFF target: send→accept→close→immediate re-send) to confirm vs a doorbell dedup regression. Also fixed same day (separate, `34bc281d6`): the **jira `transition` tool dropped its `fields` param** — `SourcegraphChatClient.xmlBlocksToToolCalls` string-wrapped object/array XML params; now parses `{`/`[`-leading values as JSON (also repairs runtime_config `env_vars` / run_maven_goal `modules`).

Resumable investigation agents (SendMessage): rootcause A=`aad861a25a2eec98c`, B=`a120c949eb2c7422b`, C=`adee5871e26d9f082`, D=`a3516dc5ccd3b3cc5`; continuation impl=`aeb5b696a5f3973b5`. Follows [[feedback_real_tdd]] + [[feedback_no_parallel_same_tree]].
