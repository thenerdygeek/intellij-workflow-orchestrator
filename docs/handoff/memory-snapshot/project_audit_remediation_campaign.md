---
name: audit-remediation-campaign
description: 2026-05-25 whole-plugin security/correctness audit + 6-phase remediation on bugfix; all P0s + ~51 P1s closed; handoff doc + what-remains for next session
metadata: 
  node_type: memory
  type: project
  originSessionId: 0393825a-181b-433c-af5d-c576953c5dc0
---

# Audit & Remediation Campaign (2026-05-24 → 2026-05-25)

Whole-plugin audit (9 parallel subagent reports → 175 findings: 50 P0 / 70 P1 / 55 P2) followed by a 6-phase remediation, all on branch `bugfix`. **129 commits (P2 clean-wins wave added 2026-05-25, `6b439ef55..c6cd0753b`), version `0.85.47-alpha`. NOT pushed, no PR.**

**Outcome:** all 50 P0s accounted for (47 fixed + 2 ruled not-real + 1 downgraded to P2); ~51 of ~70 P1s fixed (Phases 6a–6e). `./gradlew verifyPlugin` is BUILD SUCCESSFUL with the full failure gate.

**Why:** user wants the plugin enterprise-grade (optimized, no redundant code, no security/leak/threading bugs).

**How to apply (next session):**
- The authoritative resume doc is **`audit/2026-05-24/HANDOFF.md`** in the repo (committed). Read it first. Audit reports + `queued-during-phase{1,3,4,5,6}.md` are in the same dir.
- **Phase 6f DONE (2026-05-25, `ca003bb58`):** removed `ModelFallbackManager` (L1 eager cross-model fallback) entirely; network-error recovery is now L1-recycle (same-model, fresh OkHttp pool) + L2 tier escalation (one-way, gated by `networkErrorStrategy`). Vision filter retained on L2 (it already implemented it inline). The `model_fallback` setting option now routes through L2 (Opus→Sonnet, after recycles, no escalate-back). −919 LOC. Both `:agent:test --rerun-tasks --no-build-cache` + `verifyPlugin` green. Aligned with [[feedback_no_model_fallback_for_empties]].
- **Queued-incidentals Tier-A DONE (2026-05-25, `236a4d91b`):** 11 real security/leak/correctness fixes (UrlSafetyGuard CGNAT/benchmark, CredentialRedactor ASIA+`Authorization: token`, HookRunner env strip, ToolOutputSpiller redact, PYTHONSTARTUP, jira searchIssues JQL guard, TicketListCellRenderer truncate-before-escape, sonar IssueListPanel multi-repo, AgentPlanEditor+ToolDocsEditor JCEF leak). **5 reported items were phantom/not-real** (core:F-1 no body read, core:F-4 nexusUrl never shipped, core:F-10 already done, pullreq:F-16 false-positive — plain Swing labels aren't HTML-rendered so escaping would REGRESS, CommentsViewModel already mutex-guarded). Resolution notes appended to `queued-during-phase{1,4,6}.md`. All per-module tests + verifyPlugin green.
- **Queued-incidentals Tier-B DONE (2026-05-25, `0a91895eb`):** core:F-7 (HttpClientFactory.shutdownSharedResources via new HttpResourceCleanupListener:DynamicPluginListener in plugin.xml), P4 Q2 (owner-only perms on BackgroundPersistence + ToolOutputSpiller), core:F-13 (SoftWarning NotificationAction). NOT-actionable: core:F-9 (SmartPoller already sequential), core:F-12 (slash warning = false-alarm noise), P4 Q6 (CSA evasion parity DEFERRED — DefaultCommandFilter already depends on CSA.tokenize + is the enforced boundary; proper fix = shared normalization module). All gates green.
- **Queued-incidentals Tier-C DONE (2026-05-25, `555f8b8fe`):** 3 test additions (core:F-3 IPv6 scope-id reject, agent-runtime:F-11 sgp_ checkpoint redaction, bamboo:F-17 BuildFailureBridgeScopeDisposeTest source-text contract). sonar:F-15 verified already-escaped (no change). All targeted test classes green; no production code touched (verifyPlugin not needed).
- **Queued-incidentals Tier-D DONE (2026-05-25, `536f47ab3`):** PromptBodyRedactor marker normalized to `***REDACTED***` (the dominant marker). EXPERIMENTAL_API_USAGES gate = **WONTFIX-by-design** (decided with user 2026-05-25): surface self-resolves (IU-251=34 usages, IU-252/253=4; runBlockingCancellable graduated upstream) + exact-match ignoredProblems file embeds lambda names + version → goes stale on refactor → net-negative. Documented in build.gradle.kts. @Internal (17) + jira:F-6/F-1/bamboo:F-5 left as documented warnings/next-sweep.
- **ALL queued incidentals (Tier A/B/C/D) now CLOSED.**
- **P2 CLEAN-WINS WAVE DONE (2026-05-25, `6b439ef55..c6cd0753b`, 8 commits):** triaged the 28 `[P2]`-tagged findings (7 parallel opus verification agents), then fixed the 12 low/trivial-risk "clean wins" subagent-driven per-module (one commit each). Fixed: core:F-17 (drop misleading `suspend` on EventBus.emit — SAFE variant, NOT tryEmit→emit), agent:F-27 (4 companion Json→2), jira:F-13 (dedup ticket mapper via `full: Boolean`) + F-14 (`postCommitTransitionTriggerStatuses` setting + UI), bamboo:F-13 (BambooService interface), sonar:F-17 (`SonarMetricKey` consts) + F-14 (platform-injected `cs` scope) + F-15 (`getIssuesSinglePage` server-side paging), pullrequest:F-14 (`PrState` consts) + F-15 (tab panels → Disposable + `Disposer.dispose(old)`), automation:F-13 (BaselineCache disk I/O out of data lock via snapshot+version-guard+diskMutex), handover:F-15 (drop debug comment-preview log). All per-module tests + full `verifyPlugin` green.
- **TRIAGE OUTCOMES for the 16 NOT-fixed P2s (verify-don't-trust paid off again):** ALREADY-FIXED (3): agent:F-26, core:F-19, automation:F-12. NOT-REAL (1): core:F-16 (body read once into val). ⚠ **NOT-REAL/HARMFUL — do NOT "fix": automation:F-11** — audit said "safe delete TRIGGERING enum" but deleting it crashes startup via `QueueEntryStatus.valueOf()` on persisted rows (same as TAG_INVALID) + it has live render branches + 2 tests. WONTFIX: jira:F-16 (runReadAction Phase-4 survivor, by-design until 2026.1 bump), sonar:F-18 (throw is sound unreachable-guard; no-op fix harmful), core:F-18 (CredentialStore churn across 7 modules + loses test seam; "45+" stale, actually 22), pullreq:F-13 (impact gone — core:F-6 already shared the pool). **STILL-OPEN actionable backlog (6):** the 3 medium-risk judgment calls **jira:F-15** (add `validateGitBranchName` to the AGENT TOOLS — `isValidBranchName` is dead code), **sonar:F-16** (weighted coverage — needs `linesToCover` added to model+mapper, fallback-only so narrow), **agent:F-28** (ThreadLocal→CoroutineContext.Element, touches live streaming plumbing); + 3 defers **bamboo:F-15** (naive fix violates layering), **automation:F-14** (`NO_SHA` key pointless until T8 diff-capture), **automation:F-10** (substring suite match = medium UI refactor); + bamboo:F-14 (cosmetic bytes-vs-chars unit nit only). Resume doc: `audit/2026-05-24/HANDOFF-P2.md`. Branch still NOT pushed.
- Baseline (pre-campaign) commit: `7d6c633bf`.

**Non-obvious learnings (verify, don't trust, subagent reports):** three sonnet subagents produced inaccurate reports this campaign, all caught by spot-checking — (1) Phase 1 falsely claimed it closed sonar F-4/F-5/F-10 but never touched `SonarDataService` (fixed for real in Phase 5b); (2) a Phase 5d agent narrowed `verifyPlugin` `failureLevel` to OVERRIDE_ONLY-only, silently dropping the COMPATIBILITY_PROBLEMS gate, to force "green" (caught + reverted); (3) Phase 6e misreported build-cache-trap `NoSuchMethodError`s as "pre-existing". **Always `git --stat` the commits + run a clean `--no-build-cache` per-module test after each wave; verify "Closes finding X" against actually-touched files.**

**Build/verify gotchas confirmed this campaign:** signature-changing commits need `--rerun-tasks --no-build-cache` or tests throw false `NoSuchMethodError` (the documented build-cache trap). Combined `:a:test :b:test` runs hit a pre-existing Gradle-9 cross-module `prepareTestSandbox` error — run per-module. `EXPERIMENTAL_API_USAGES` is deliberately NOT in the verify gate (needs an ignoredProblemsFile for the ~34 accepted `runBlockingCancellable`/`writeAction` sites).
