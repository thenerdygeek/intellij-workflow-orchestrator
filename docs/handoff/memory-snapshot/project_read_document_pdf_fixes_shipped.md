---
name: project_read_document_pdf_fixes_shipped
description: "read_document robustness: audit (125 findings) + all 5 P0s + 7 P1s + SF-1 + PPTX/HTML/DOCX/figure follow-ups. PUSHED + RELEASED v0.85.47-alpha-cross-ide.14. runIde/live-agent smoke pending (user checking)."
metadata: 
  node_type: memory
  type: project
  originSessionId: d5b282ab-79b2-4587-b3c7-0c417d92853f
---

## UPDATE 2026-05-29 — cross-format robustness audit + 5 P0 fixes (COMMITTED, NOT PUSHED)
Built a format-agnostic `CorpusExtractionProbe` (test harness, hardcoded `/tmp/rd-corpus/inputs`→`/out`; Gradle won't forward `-D` to the test fork) + ran a Workflow (read-only): downloaded a 21-file corpus (PDF/DOCX/XLSX/PPTX/HTML/CSV/JSON/MD), extracted once, analyzed each vs ground truth, adversarially verified → **125-finding gap report** `docs/superpowers/specs/2026-05-29-read-document-robustness-gap-report.md` (commit `d3009097e`). Then fixed all P0s (TDD, subagent-driven, one commit each):
- `044f8fd78` **G-2**: URL_BOUNDARY space-injection was a PDF-only workaround running on ALL pipelines → corrupted `"url":"https://"`→`" https://"` in JSON/HTML/MD. Gated behind `restoreUrlBoundaries` (PDF-mime only); verbatim is default.
- `e9de8db1d` **G-8 P-2/P-3**: XLSX merge-spill (anchor value duplicated into every spanned cell → fabricated data) + cached `<v>` discarded by live re-eval (1→0). Fix: spanned cells blank + `Merged ranges:` note; honor cached value when `isSetV && v not blank`.
- `a2661ab07` **G-1 (NAV-1)**: `isExtractMarkedContent=true` made Tika, on TAGGED PDFs (Acrobat PDFMaker/Word), emit all prose flat then dump empty page-divs at the tail → 99% of prose got page 1. Set it `false`. Page-1 body share 83%→3%. New `TaggedPdfFixtureFactory` (untagged PDFs don't repro).
- `cb682ba91` **G-3 (T-1/2/3/4)**: Tabula full-containment cell fill DROPS glyphs straddling a column ruling (`4,558,150`→`4,558,15`, `1994`→`994`). Fix: re-derive cell text by CENTER-POINT containment. + skip leading blank spacer rows (whole-table drop). T-3 conflicting-numbers resolved as side effect.
- `72c87c34c` **HX-1**: link-anchor hoisting (`Section 5.1.7Section 5.1.1`, `FIPS 140`×4, empty `()`) = SAME `isExtractMarkedContent` flag as G-1 → already fixed by `a2661ab07`; added mechanism-true guard test + KDoc fixes.
All on `:document` only; `:document:test` (319) + `verifyPlugin` (IU-251/252/253) green.

## UPDATE 2026-05-29 (same session) — P1 backlog SHIPPED (7 commits), all committed NOT pushed
After the P0s, ran the full P1 backlog (TDD, subagent-driven, one commit each), `:core`+`:document`+`:agent`+verifyPlugin all green:
- `5a02b9438` **G-5**: seed section index from PDF outline (`PDDocumentOutline`→Heading, depth=level) + demote noisy heuristic headings when outline exists + NAV-3 promotion guards + SF-9 title-artifact. 800-53r5 levels 308H1/19H2/0H3→11H1/31H2/316H3.
- `9c0a64c5b` **G-4**: HTML charset — tika-config.xml had forced ONLY UniversalEncodingDetector (prefix-sampling→windows-1252 on deep multibyte). Restored ordered chain (StandardHtmlEncodingDetector→new HtmlDefaultUtf8EncodingDetector→Universal). NAV-2 was NOT a separate bug (anchors already char-offsets; drift was char-vs-byte on mojibake). New `tika/HtmlDefaultUtf8EncodingDetector.kt`.
- `3e93eaf31` **G-6**: harvest PDF `/URI`+`/Link` annots → inline markdown links (`PdfMetadataExtractor.extractLinks` via PDFTextStripperByArea; `PdfPipeline.spliceLinksIntoProse`). External `[t](uri)`, internal `[t](#page-N)` (page-N not slug — slug mapped every citation to #conclusion). Conservative: un-link rather than corrupt.
- `292cc17f0` **G-10** (the search feature, user chose `search=` param): ranked snippets `[page·§section·offset]`. Layered core(`DocumentSearchMatch/Result`+`sectionAt`)→document(`DocumentSearchEngine` pure)→agent(`DocumentTool` param + `SessionDocumentArtifactService.search`, shared `resolveArtifact`).
- `9944111ef` **G-8 remainder**: XLSX formulas `=SUM(..) (val)` (P-1), header-evidence heuristic else positional Col A/B (P-5), defined tables (P-6), chart type+source labels (P-7); P-4 = P-3 note suffices.
- `996688fed` **G-7**: image alt-text from docPr/cNvPr descr (IMG-1), `EmbeddedObjectRef` placeholders `[SmartArt:]/[Embedded object:]/[Shape:]` (IMG-3), marker reconciliation image→`[image:` / non-image→`[embedded:]` (IMG-4, html-gdp 0→238). ⚠ `:core` DocumentBlock signature changed (altText + new subclass) — run :agent with `--no-build-cache`.
- `6efc2ca22` **G-9 PARTIAL**: SF-5 header/footer band strip, SF-4 glued function words + U+FFFD bullets, SF-3 TOC dot-leaders, SF-10 de-hyphenation. Conservative guards + normal-text-untouched tests.
## UPDATE 2026-05-29 (same session) — 3 AGENT-FEEDBACK fixes + crash fix; RELEASED .15
Agent live-smoke of .14 reported 3 real read_document nav limitations; root-caused HOW the audit missed them (corpus was PUBLIC docs not the user's actual API-spec doc; probe tested extraction fidelity + heading-lookups only, never table-by-name/deep-subsection nav). User: fix all 3; can't share sensitive doc → acquired PUBLIC structural analogues (DICOM PS3.4/3.7/3.18 REST specs, Microchip PIC18 datasheet w/ literal "(CONTINUED)" tables + 4-deep outline). Fixes:
- `900c790bd` **#2 multi-page table stitching** + **CRASH FIX**: enableStreamMode=true (our Task-2 default) made Tabula SpreadsheetExtractionAlgorithm throw on skewed-ruling pages, unguarded throw ABORTED whole-doc table extraction (microchip got NO tables). Per-page runCatching isolation. mergeContinuations rewritten: currentLastPage chaining (was testing start-page → only merged pairs), 3 signals (literal "(continued)"/repeated-header/blank-dominant silent-split), anti-fusion guard. microchip TABLE 1-2 4→1, 1-3 6→1; DICOM silent splits merged. extractRaw stays per-page (SF-1/SF-2 gates).
- `d758593cc` **#1 table-level navigation**: new `DocumentIndex.tables` anchor list (MarkdownAssembler detects `Table N.`/`TABLE 1-2:`/`8.3.2-1.`/`B.2-1.` captions; requires ./: separator to reject prose refs); section= resolves tables (heading-first, tolerant); `availableTables` (own 30-cap) + `[Tables:]` hint. DICOM 173 table anchors.
- `2eb19e4f9` **#3 deep subsection anchoring**: demoteProseHeadings now ADDITIVE — keeps heuristic numbered headings (^\d+(\.\d+){1,4}) NOT in outline coverage → 2.4.4.1 anchored when outline stops at 2.4.4; demotes non-numbered noise (NAV-3 intact). Cap 30→200 + totalSectionCount + "(showing N of M)" + number-prefix miss-bias. DICOM 9.1.1.1.5 anchored; G-5 baselines unchanged.
All :core/:document/:agent + verifyPlugin green. **RELEASED v0.85.47-alpha-cross-ide.15** (pushed @ bec49f844). ⚠ LESSON: test on representative REAL docs, not just diverse public ones; audit extraction-fidelity ≠ navigation-UX. STILL DEFERRED: fully-populated-data silent table splits; SF-8 masthead; arxiv display equations; >200-section truncation.

## UPDATE 2026-05-29 (same session) — followup backlog cleared (3 commits AFTER release .14, NOT yet pushed/released)
After cutting .14, did the deferred followups (TDD, sequential, committed each): grouped/merged 2-row table headers `9cc640118` (composite flatten, 6-cond conservative gate; corpus reach limited — fed-scf caption-fused/BJS mis-gridded/arxiv-Table-2-not-extracted deferred); **SF-2 preformatted** `dd9e139ed` (new `DocumentBlock.CodeBlock`; PdfPreformattedDetector via PDFBox monospace-font + column-non-filling + tight-spacing; rfc7230 73 CodeBlocks fenced — ABNF/pseudo-code/ASCII; byte-identical guard for prose docs); **IMG-6 glyph-noise** `b5888f982` (suppress image when smaller-dim<80px AND no caption; 800-63b imageMarkers 69→13; caption/size protect). :document+verifyPlugin green. These 3 are NOT in release v0.85.47-alpha-cross-ide.14 — offer a .15 once user finishes checking .14. **DELIBERATELY DEFERRED (poor ROI, engineering call):** SF-8 masthead (cosmetic) + arxiv display-equation 2D math reconstruction (research-grade, narrow benefit).

## UPDATE 2026-05-29 (same session) — PUSHED + RELEASED
Branch `feature/cross-ide-delegation` pushed to origin @ `7c71857a6`; release **v0.85.47-alpha-cross-ide.14** (pre-release, ZIP attached) cut for the user to live-smoke read_document via the agent. Also shipped post-SF-1 follow-ups: PPTX hyperlinks (`740a1c7ce`), HTML chrome-strip/cite/icon-wall (`50982eeaf` — ChromeStrippingHtmlMapper.isDiscardElement for nav/header/footer/aside), DOCX footnote/endnote namespacing `[^fnN]`/`[^enN]` + mc:AlternateContent shape recovery (`fd1763549`), PDF figure-caption alt-text via PDFGraphicsStreamEngine geometry (`a... IMG-2`). STILL OPEN: grouped/merged 2-row table-header reconstruction; SF-2 preformatted + SF-8 masthead; IMG-6 fragment-threshold tuning. ⚠ user paused the remaining backlog to check the release first.

## UPDATE 2026-05-29 (same session) — SF-1 SHIPPED (the "big" deferred item; turned out medium)
`4c830956c` design doc + `e8bc2722a` impl. Spike proved only bjs-cv22 is truly 2-col (1/9), so NOT a Tika→PDFBox rewrite — a bolt-on: `PdfColumnDetector` (glyph-coverage valley histogram, center-band-vs-flanks, ≤0.15 ratio, conservative) + `PdfColumnProseExtractor` (PDFTextStripperByArea region split) + `PdfPipeline.substituteTwoColumnPages` (fires only on 2-col non-table pages). Guardrails verified: Tabula gate via new `PdfTableExtractor.extractRaw` (pre-merge page set — multi-page tables collapse to start page, so raw needed to protect nist-csf's 4-col table); single-col docs byte-identical (block counts on==off). No fork — extracted `DocumentBlockHandler.processProseLine` shared util. bjs page2 28 fused→130 ordered, G-6 links 13→13. :document+verifyPlugin green. ⚠ user said SKIP worktree-based parallelism (sequential only). Concurrency rule reaffirmed: worktree.baseRef unset → Agent isolation:worktree branches from origin/main (misses cross-ide commits); manual worktrees from HEAD needed for safe parallel — user opted out.

**STILL DEFERRED (open follow-ups, smaller P1/P2):** SF-2 preformatted (ABNF/code fencing) + SF-8 masthead (need intra-line position data); grouped/merged 2-row-header table composite reconstruction; IMG-2 PDF figure↔caption positional; PPTX hlinkClick + footnote-namespacing (HX-2) + HTML #cite_note; IMG-5 icon wall; IMG-6 fragment-threshold tuning; HTML nav/boilerplate strip; DOCX mc:AlternateContent shapes. Cosmetic residuals: watermark-glyph bleed into table cells; arxiv github URL as 2 adjacent links. **runIde smoke still pending across all.**

---

**COMMITTED (NOT PUSHED) 2026-05-29** on `feature/cross-ide-delegation` (worktree `.worktrees/cross-ide`): three root-caused `read_document` PDF extraction bugs fixed, all via TDD, subagent-driven (implementer-only).

- `12945b62c fix(document): repair read_document PDF section nav, borderless tables, image MIME` — Task 1 section nav (standalone numbered+unnumbered heading detection in `DocumentBlockHandler`; 3-tier `offsetForSection` exact→number-stripped→substring in `DocumentArtifactModels`; miss banner + section hints in `DocumentTool`); Task 2 borderless tables (`PdfTableExtractor` per-page Tabula stream fallback when lattice empty + strict `isStreamPhantomTable` guard ≥2cols/≥2rows/no-ragged-tail/≥0.7 fill; `PdfPipeline` default `enableStreamMode=true`); Task 3 image MIME (`ImageExtractionService.sniffImageMime` magic bytes + `saveImage` <32px fragment filter; all 4 image callers migrated).
- `432225d73 docs(tdd-skill): ...RED failure reason...` — separate commit for the unrelated `tdd/SKILL.md` enhancement (per user decision to not bundle it).

Verified: `:document:test` (304+probe), `:core:test`, `:agent` DocumentTool/SessionDocumentArtifactService tests, `verifyPlugin` (IU-251/252/253) ALL green. Probe vs ground truth: section anchors 800-63B 3→131, RFC7230 0→95; exact+slug hits ✅. `natural-hit ❌` is a probe artifact (realKey now unnumbered), not a gap.

⚠️ **PENDING:** not pushed; **runIde smoke pending** (validate Task 2 phantom guard 0.7/ragged-tail against a real spec corpus — risk of over-dropping genuinely-sparse borderless tables). Full detail in committed handoff `docs/superpowers/specs/2026-05-29-read-document-pdf-fixes-HANDOFF.md`. Builds on [[project_document_extract_once_indexed_read]].
