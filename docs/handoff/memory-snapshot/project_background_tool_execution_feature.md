---
name: background-tool-execution-feature
description: SPECCED+PLANNED (not built) 2026-06-21 — run ANY work tool in the background; worktree feature+background-tool-execution
metadata: 
  node_type: memory
  type: project
  originSessionId: 2375d000-726c-45c3-8294-b20be11b6f2d
---

**SPECCED + PLANNED, NOT BUILT — 2026-06-21.** Feature: any *work* tool can run in the background, two triggers — agent-initiated (`<run_in_background>true` loop-level attribute) and user-initiated ("Move to background" button on a running tool card). Result delivered async via the existing `UnifiedMessageQueue` BACKGROUND source (auto-wakes idle session), modeled on the monitor framework.

**Worktree:** `.claude/worktrees/feature+background-tool-execution`, branch `worktree-feature+background-tool-execution`, branched fresh from `origin/main` @ `cf3d22873` (#62). Spec v3 committed (`a16419620`, on top of `d337a39fd`/`862d5c952`/`55f6e98f1`). Plan is **gitignored→local only**: `docs/superpowers/plans/2026-06-21-background-tool-execution.md` (76k, 9 TDD tasks). Spec: `docs/superpowers/specs/2026-06-21-background-tool-execution-design.md`.

**Locked decisions (user):** D1 full-parallel; D2 all work tools, control-flow denylisted; D3 monitor-style — never blocks attempt_completion, late result auto-wakes; D4 loop-level reserved attribute (NOT per-tool param — token budget); D5 cap 5 configurable; D6 on-by-default + kill switch; D7 sub-agent `agent` tool IS eligible (user overrode my "exclude"); D8 survive-on-session-switch, cancel-on-new-chat.

**Architecture:** "launch every eligible tool in a per-session SupervisorJob scope, await inline, detach = stop awaiting." New: `BackgroundToolExecutor`/`BackgroundToolHandle`/`BackgroundToolRegistry`/`BackgroundEligibility` + pure `ToolOutputProcessor` (extracted from AgentLoop:2111-2137). Reuses #62 `ToolCancellationRegistry` (Stop still works on bg tools) + #61 queue/async-cards.

**Adversarial Opus review (agentId a2cf996377195cc21) found 3 blockers + 5 important, ALL verified true against code + folded into v2/v3:** (1) `ToolCall(id,type,function)` carries params in `FunctionCall.arguments` JSON string → NO struct field; read+strip from decoded `params`. (2) `allParamNames()` is registry-derived → augment reserved tag at AgentService:734/1901/2464 + SubagentRunner. (3) per-session executor collides with single-active-task model (`activeTask` AtomicReference; new task cancels prior) → AgentService-owned, session-keyed; cancelAllForSession on new-chat/task-cancel/dispose; auto-wake is CONDITIONAL (same-session-idle wakes; other-foreground defers to durable-queue replay — `IdleSessionWaker` DEFER_ACTIVE_SESSION). (4) drain is `addUserMessage` (AgentLoop:943-952) NOT addToolResult → bg results deliver as framed user text like DELEGATION/MONITOR; POST_TOOL_USE + diff accounting do NOT fire for bg tools (lossless for real candidates — they emit no diff). (5) ThreadLocals = 3 (`BackgroundProcessTool.currentSessionId` + `RunCommandTool.currentToolCallId`/`currentSessionId`) × 6 STREAMING_TOOLS + `runWithStore` ctx → set INSIDE executeOnce (the open `agent:F-28` issue). D7 verified sound: `executeParallel` supervisorScope{async{runner.run()}} + descendant coroutineScope → cancelling the bg agent-tool job tears down sub-agents.

**Doc lives in :core** `ToolPromptBuilder.FORMAT_INSTRUCTIONS` (1 bullet; sub-agents parse-and-strip, don't honor). Settings: `allowToolsRunInBackground`(true)/`maxBackgroundedToolsPerSession`(5) — named distinct from existing `concurrentBackgroundProcessesPerSession`. ⚠ Task 6 changes a ctor lambda to `suspend` → run `--no-build-cache`. ⚠ touching FORMAT_INSTRUCTIONS regenerates ALL prompt snapshots.

**✅ ALL 9 TASKS IMPLEMENTED + WHOLE-BRANCH REVIEWED → PR #66 OPEN (2026-06-21).** Pushed `worktree-feature+background-tool-execution` HEAD `e4da3f85e` → `origin/feature/background-tool-execution`; PR #66 vs main (https://github.com/thenerdygeek/intellij-workflow-orchestrator/pull/66). 19 commits (4 spec docs + 9 SDD tasks + 6 review fixes); 30 source files +1029/-108. New pkg `agent/.../tools/background/` (coexists w/ prior background-PROCESS infra) + `loop/ToolOutputProcessor.kt`.

**Execution = SDD (subagent-driven):** Tasks 1-4 hand-implemented by controller (session-limit had zeroed the implementer subagents); 5-9 by sonnet/opus subagents (Task 6 AgentLoop surgery = opus) + per-task reviewer + fix loop. Per-task review fixes: FORMAT_INSTRUCTIONS sub-bullet indent, unused-import-baselined-not-deleted, settings blank-line, empty async-card details/spillPath. ⚠ caught: `ToolOutputConfig`/`Spiller` in `.tools.` NOT `.tools.output.`; `@OptIn(ExperimentalCoroutinesApi)` for getCompleted; AsyncEventCardPresenter.fromBackground takes a BackgroundCompletionEvent (build AsyncEventCardData directly + encodeCard); Task 9 subagent didn't run :agent:detekt (formatting slipped, baselined the _moveToolToBackground mirror like _killToolCall).

**⚠ FINAL WHOLE-BRANCH REVIEW (opus) found 2 concurrency races invisible to single-threaded runTest — BOTH FIXED (`e4da3f85e`):** (1) `lateinit job` publish-before-assign → `cancelAllForSession` (EDT) could hit uninitialized lateinit = hard crash → fixed w/ `CoroutineStart.LAZY` (assign+register+invokeOnCompletion+add THEN job.start(); also closes fast-completion remove-before-add leak). (2) detach-vs-completion double-delivery TOCTOU → fixed w/ single-owner `BackgroundToolHandle.claimDelivery()` AtomicBoolean CAS'd by both the loop select + executor invokeOnCompletion. Lesson: runTest = single-threaded virtual time, can't surface these — whole-branch review (real-thread reasoning) is the net.

**Tests:** full clean :agent:test (~5480, --no-build-cache) + :core:test + :agent:detekt + verifyPlugin + webview vitest all GREEN. **⚠ IN-IDE SMOKE DEFERRED** (like #62 — needs live IDE + SG token): agent-backgrounds-tool, click move-to-bg, completion auto-wake post-attempt_completion, backgrounded sub-agent, kill-switch-off. Degrades to unchanged inline path so low-risk. **NEXT:** CI + in-IDE smoke, then merge. Worktree preserved for PR iteration. Related: [[project_unified_tool_stop_feature]] (#62 cancel infra), [[project_run_command_auto_approve_research]] (#65, parallel feature), [[feedback_subagent_model_no_haiku]].

**/simplify pass (2026-06-21, head `40e060775`):** 4 parallel cleanup agents → applied: `BackgroundEligibility.withReservedParams()` (DRY 5 paramNames sites, 3-agent consensus); `AsyncEventCardPresenter.fromToolResult()` factory (card built in presenter, single clock read); `humanizeMs` in renderBackgroundTasks; dropped needless `suspend` from ToolOutputProcessor; removed write-only `params`/`tool` from BackgroundToolHandle. Skipped (noted in commit): FQCN→imports (ImportOrdering baseline churn), registry micro-nits (cap=5), webview hoist (dist-rebuild cost), alreadyProcessed rewrite (loses raw result). Full clean :agent:test+:core:test+:agent:detekt GREEN; pushed to PR #66.

**✅ MERGED 2026-06-21: PR #66 squash-merged to main; all 5 CI checks green (Tests passed despite the flaky-CI history #51; Build&Verify 5m23s; Kover 12m37s). Remote branch deleted; local worktree preserved (cleanup at leisure). ⚠ in-IDE smoke STILL deferred (live IDE + SG token) — merged on the #62 additive+graceful-degrade precedent.**
