---
name: perf-audit-2026-06-10
description: "Perf campaign COMPLETE 2026-06-11 — Waves 1+2 merged (#53/#55/#54); Waves 3-6 = PR #57 (open, single PR, all gates green); next = profiler/diagnostics feature"
metadata: 
  node_type: memory
  type: project
  originSessionId: 37b178d2-5a41-4b4a-bef6-f8fb08cabf7f
---

2026-06-10: Completed a 6-domain performance audit (agent backend, JCEF bridge, React webview, :core, background work, feature Swing). Report: `audit/2026-06-10-perf/PERF-AUDIT.md` (in repo, uncommitted). User goal: plugin must run on Intel i3 (lags even on Core Ultra 7) or marketplace rejection.

**Why:** Fix campaign approved (scope = EVERYTHING incl. P2s+bugs, user choice 2026-06-10). Plans exist — do NOT re-audit or re-plan Wave 1; read the plan files.

**Plans:** `docs/superpowers/plans/2026-06-10-perf-campaign-overview.md` (6 waves, every finding mapped, coverage-checked; NOTE: plans dir is gitignored — local only) + `2026-06-10-perf-wave1-quick-wins.md`. Waves 2-6 plans are written JUST-IN-TIME when the prior wave merges. Profiler/diagnostics feature comes AFTER the campaign (user decision).

**WAVE 1 DONE 2026-06-10 → PR #53 (perf/wave1-quick-wins, NOT yet merged):** P0-4, P1-6, P1-7, P1-9, P1-10, P1-11, P2-1 + bugs B1, B12, B14 — 11 commits incl. detekt style pass. Full tests + verifyPlugin + detekt green; per-task two-stage reviews + final integration review passed. Manual smoke checklist is in the PR body (4 items, user-run). Audit report's "Fix status" section tracks SHAs. NEXT: Wave 2 = streaming/persistence cadence (P0-1, P1-1..5, P2-4..6, bugs B2/B3/B6/B15/B17/B18) — write its detailed plan against fresh code AFTER #53 merges; use Fable/Opus-tier implementers there (MessageStateHandler data-loss risk), not Sonnet.

**WAVES 3-6 → PR #57 SQUASH-MERGED to main 2026-06-19 `b873f8c82` (perf/waves3-6 branch deleted; the PasswordSafe token-persist fix `fe5f267cf` rode in on the same PR; main pluginVersion now 0.87.2). 7-item in-IDE smoke STILL pending. Original close-out (22 commits, OPEN at the time):** all remaining tasks closed (C2 review+fix `ec2e63131`, C3 `0aa732646`+pins `ea186c82b`, D1 `ddaca24b7`, D2 `99f1a3d6c`, D3 `f65d18bf3`+fix `3edaa0a90`, docs close-out `f87ea7ecb`, bridge fix `91d8980b6`). Final gate: 8796 Kotlin tests/0 failures `--no-build-cache`, detekt 0 all modules, verifyPlugin Compatible, webview vitest 752/tsc/build + preload contract. Audit ledger: no findings OPEN (P2-3 accepted, P2-8 rejected, P2-23 skipped, P2-11/P2-12 partial). 7-item smoke list in PR body. The review loop caught Criticals on D-track too: D1's first leaf-only gate would have painted one icon PER TOKEN, and its header cache served wrong-branch coverage — never skip renderer-sweep reviews. Integration review found the PRE-EXISTING sub-agent thinking bridge bug (string payload never JSON.parsed → key "undefined") — fixed on-branch. FOLLOW-UPS (post-merge): P2-11 webview ticker adoption of `__wfToolWindowVisible`; AgentConfigLoader `stopWatching`/dispose not `@Synchronized` (narrow fd-leak race); sonar header cache not invalidated on purely-local git branch switch; D2 detail-gate freezes LAF fonts on quiet entries. NEXT after merge: profiler/diagnostics feature (perf snapshot button + PerfCounters — sketch in 2026-06-10 conversation).

**B24 (user-reported live crash, 2026-06-10) → PR #54 (fix/delegation-project-closing-edt, unmerged):** `DelegationInboundStartupActivity.projectClosing` called `runBlockingCancellable` on EDT (window-close fires there) → IllegalStateException → `closeAllForProjectClose()` never ran. Fixed: branch on `isDispatchThread` → EDT uses `runWithModalProgressBlocking` (com.intellij.platform.ide.progress), BGT keeps runBlockingCancellable. Pinned by DelegationProjectClosingEdtContractTest. LESSON: projectClosing/message-bus listeners fire on EDT for window closes — `runBlockingCancellable` is NOT EDT-safe (it asserts BGT since 2025.x); grep other projectClosing/projectClosed listeners for the same pattern when touching delegation.

**ALL MERGED 2026-06-10 (user authorized after minimal hi-hello smoke):** #53→`4e7d6e67e`, #55→`cc07d82ce` (rebased onto post-#53 main, clean), #54→`da9e7c744`; all squash; branches deleted (release tag v0.86.0-perf.1 keeps its commit reachable). Main's pluginVersion is now 0.86.0-perf.1. ⚠ Cancel/resume + focus-gate behaviors were NOT explicitly smoke-tested in-IDE — if a field report mentions lost partial text after cancel or stale background tabs, start at MessageStateHandler flush boundaries / SmartPoller.defaultIdeFocused.

**WAVE 2 DONE 2026-06-10 → PR #55 (perf/wave2-persistence-cadence, STACKED on #53 — retarget to main after #53 merges):** all 7 tasks + review-minor commits landed (10 commits, bc2816ed9..HEAD incl. docs). Fixed P0-1, P1-1..5, P2-4 + bugs B2/B3/B6/B15/B17/B18. P2-5 + P2-6-partial DEFERRED (rationale in audit Fix status). Full sweep + verifyPlugin + detekt green; per-task two-stage reviews all APPROVED; final integration review dispatched. Manual smoke (user): ui_messages.json mtime ≤2/s during streaming; cancel-mid-stream→resume intact. NEXT: Wave 3 = webview (P0-2 manualChunks, P0-3 sub-agent stream re-render, P1-13..16, P2-13..17, bugs B8/B9/B10/B22) — plan JUST-IN-TIME after #53+#55 merge; Sonnet implementers fine there (TS/React, no data risk).

Wave 2 learnings: kotlinx CharSequence extensions are drop-in for String parsing code (clamping semantics identical — verified); adding a defaulted ctor param to a class used across test bytecode trips the build-cache trap (needs :agent:clean, not just --no-build-cache); SendMessage-resumed worktree agents may restart in the MAIN tree cwd (T7 did) and my own Bash cwd can drift into worktrees — always cd to repo root explicitly; clock-seam constructor params (`uiSaveClock`) are the repo's pattern for deterministic throttle tests.

Notable Wave 1 learnings: javax.swing.Timer doesn't tick in the IntelliJ headless test harness (tests must drive flushIfNeeded directly); bare WatchService.take() trips ThreadLeakTracker on scope.cancel — use `runInterruptible { take() }`; this repo dual-registers @Service(APP) classes in plugin.xml by convention (NOT an error on 2025.1 — verified against platform binaries); :core has detekt/ktlint CI gate — new code must pass `:module:detekt`.

**How to apply:** The four root causes: (1) per-SSE-chunk full-file rewrite of ui_messages.json (O(n²)) + per-message api_history rewrite + per-write-tool LCS diff (100MB DP arrays); (2) webview vite `manualChunks` defeats ALL lazy loading → ~8MB eager JS, + sub-agent streaming rebuilds whole messages[] per 16ms batch; (3) SmartPoller `isIdeFocused()` tautology (`lastFocusedFrame != null`) kills focus gating plugin-wide + always-on 200ms/500ms agent loops + 30s Haiku LLM calls; (4) extension tabs bypass lazy-tab → eager Chromium + browser/controller LEAK per "Refresh All Tabs" (AgentDashboardPanel not Disposable).

Key verified-clean: all 5 old runBlocking-on-EDT AgentController sites FIXED (skill/docs stale); main prose streaming + virtuoso + EventBus + HttpClientFactory all correct.

Top bug: [[B1]] AgentConfigLoader app singleton disposed by first project close (kills personas for other projects). Also: unclickable artifact Download buttons (JButtons in JList renderer), MessageStateHandler CME races, mirror replay cap inverted (keeps oldest).

Suggested fix order is at the bottom of the report (quick wins → streaming/persistence cadence → webview → lifecycle → renderer sweep → bugs).
