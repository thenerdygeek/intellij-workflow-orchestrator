# Phase 2 SDD progress — carve company-only → B

BASE (pre-Phase-2, for final whole-branch review): dad8d4c7bf89712b05a57bcbd4edae4aecf2c465
Branch: feature/plugin-split
Charter: docs/superpowers/specs/2026-06-27-plugin-split-phase2-handoff.md
Spec: docs/superpowers/specs/2026-06-22-plugin-split-design.md (REV 3, §16–20)
Scope map (Phase 1): .superpowers/phase1/phase1-scope-map.md

(Phase 1c COMPLETE @ f6bb37636 — archived in git history + memory. Earlier 1c ledger overwritten here; recover from git log if needed.)

## Decomposition (charter §4 suggested; may refine)
- 2a — carve :automation → B (tab/tools/services/settings/statusbar/startup via EPs; A degrades when absent)
- 2b — carve :handover → B (tease CopyrightFix year-logic into A; HandoverConfigurable in :core; dead PreReviewService reg)
- 2c — B config-preset / WorkflowConfig widening + deferred blanking (bambooBuildVariableName, quickClipboardChips)

## Process per sub-phase (charter §5)
explore (.superpowers/phase2/explore-*.md) → plan (bite-sized, char-test-first) → 2–3 opus plan reviews → revise+confirm
→ SDD execute (impl + independent task-reviewer per task; controller verify) → whole-branch opus review → green gate → docs → commit.
No push. No Co-Authored-By trailer.

## Pre-existing facts to carry (verified 2026-06-27)
- A bundles modules via root build.gradle.kts implementation(project(":automation"|":handover")) lines 208/209 (+ kover 223/224).
- :automation deps: :core + :bamboo (+ trimmed sqlite-jdbc). :handover deps: :core only.
- :core-resident tendrils: HandoverConfigurable (settings UI), PrService (core.bitbucket, generic?), DockerTagsProvider (core.model, consumed by :bamboo run config), PluginSettings fields (quickClipboardChips, bambooBuildVariableName), WorkflowEvent neutral event types (Automation*/Handover*), BuildLogCache (automation ref).
- plugin.xml:369 registers handover.service.PreReviewService which DOES NOT EXIST in main tree (dead reg; drop on carve).
- EXCLUDE from all searches: /build/, .worktrees/, .claude/worktrees/ (stale copies).
- B already wired: CompanyBWorkflowConfig, CompanyBToolContributor, CompanyBSettingsConfigurable; plugin-b build has compileOnly(:core)/(:agent), localPlugin(root), verifyPlugin disabled.

## Architecture decisions (from exploration; controller, 2026-06-27)
- **D1 Physical layout:** KEEP `:automation`/`:handover` as TOP-LEVEL Gradle modules. B BUNDLES them via `implementation(project(":x")){isTransitive=false}` (ships only their own classes; :core/:bamboo resolve at runtime via A's parent classloader since B `<depends>` A). A stops bundling them. Rationale: minimal-diff, reversible, and konsist `ModuleBoundaryTest`/`LayeringTest` keep checking them (their `filterNot{/plugin-b/}` would drop coverage if dirs moved under plugin-b/).
- **D2 Tool-window degrade:** `WorkflowToolWindowFactory.defaultTabs` (private val, lines 368-375) HARDCODES "Automation"(4)+"Handover"(5) → A renders empty ghost tabs even with no provider. MUST remove "Automation" in 2a, "Handover" in 2b. After removal, B's tabProviders surface as extension-provided (pass-2 lazy) tabs ordered by `order`.
- **D3 sqlite:** A does NOT ship sqlite (root build line 231 comment: removed). Only :automation ships trimmed sqlite-jdbc. B MUST replicate the trimmedSqliteJar machinery in plugin-b/build.gradle.kts (isTransitive=false won't carry automation's `files(...)` dep). okhttp: A ships it via :core → B need NOT ship it.
- **D4 No agent tools:** automation+handover have ZERO AgentTools; reconnection = tabProvider + projectService(@Service auto-reg) + projectConfigurable(parentId) + statusBar + postStartupActivity + notificationGroups. agentToolContributor EP NOT on critical path.
- **D5 Dead/misfiled regs:** DROP dangling `automation.service.ConflictDetectorService` (plugin.xml:337-8, class doesn't exist) and dead `handover.service.PreReviewService` (369). MOVE `core.bitbucket.PrService` (371) out of handover block — it's generic :core, consumed by :pullrequest, STAYS in A.
- **D6 CopyrightFix:** generic; move `CopyrightFixService` + `CopyrightFileEntry`/`CopyrightStatus` to :core (core/copyright/, next to CopyrightCheckService); `CopyrightFixCard` UI → B (with :handover); company template stays a blank :core setting `copyrightTemplate`, B seeds it (2c).
- **D7 HandoverConfigurable:** move from :core INTO :handover (handover-only); B registers via projectConfigurable parentId="workflow.orchestrator". quickClipboardChips/aiSummariesEnabled/handoverOverrideLog FIELDS stay neutral in :core PluginSettings.
- **D8 Config preset (2c, the hard part):** lean toward a NEW project-scoped `@InternalApi ConfigPreset` EP (not widening @StableApi WorkflowConfig). B-driven first-launch seed with "==neutralDefault" don't-clobber guard, ordered AFTER A's SettingsMigration. Surfaced as DECISION-NEEDING-CONFIRMATION candidate. Blanking bambooBuildVariableName + quickClipboardChips docker entries lands here too.
- **Headless gate proxy for duplicate-class trap:** build :plugin-b:buildPlugin, UNZIP dist, assert B's jar contains automation/handover classes but NOT :core/:bamboo/okhttp (cross-classloader runtime = PENDING-USER runIde smoke).

## Tasks
- [x] Exploration (3 parallel agents) → explore-automation.md, explore-handover.md, explore-ep-preset-konsist.md (DONE)
- [x] 2a PLAN written + reviewed: `docs/superpowers/plans/2026-06-27-plugin-split-phase2a-automation-carve.md`. 3 opus plan reviews (correctness/completeness=NOT-READY→fixed C1+C2; architecture/OSS-skeptic=READY-WITH-FIXES; gradle-build-graph=READY-WITH-FIXES, empirically proved isTransitive=false works) → fixes folded → confirming re-review = READY (3 stale cross-refs fixed). Key fixes: C1 konsist JUnit5 assertions (not kotlin.test); C2 TrimmedSqliteJar test relocates IN Task 3; keep kover(:automation); record bamboo.ui coupling + bambooBuildVariableName-defer; correct sqlite rationale (A ships it transitively today).
- [ ] 2a EXECUTE (6 tasks SDD) + gate + commit
  - [x] T1 complete (commit b82432abe; SPEC ✅ + Approved, 0 Crit/Imp; konsist contract test RED→GREEN, JUnit5 Assertions, WorkflowToolWindowFactory detekt-clean). MINOR: unused `assertTrue` import in Phase2AutomationCarveContractTest until Task 4 adds assertTrue calls — self-resolves; verify :konsist detekt at end-of-2a gate only.
  - [x] T2 complete (commit 489ad47e7; SPEC ✅ + Approved opus, 0 issues; impl-only removed, kover kept, dangling ConflictDetectorService dropped, all non-automation neighbors retained; konsist 4/4 GREEN, verifyPlugin BUILD SUCCESSFUL, A-dist jar check 0 automation + 0 sqlite. ⚠ buildSearchableOptions needs `-x` when an IDEA instance is running on macOS — env quirk, not a failure; use `-x buildSearchableOptions` at the gate if needed.)
  - [x] T3 complete (commit 76e64462e [controller-committed: implementer made changes but ran out of turn on the slow build before committing/verifying; controller did independent verification + commit]; SPEC ✅ + Approved opus, 0 issues). CRITICAL jar-content check PASSED: B has 173 automation classes + sqlite, 0 of :core/:bamboo/okhttp → isTransitive=false works empirically, no duplicate-class trap. :automation:test + :plugin-b:test GREEN. ⚠ runtime cross-classloader (automation→bamboo.ui via A parent CL) = PENDING-USER runIde smoke. ⚠ git add aborts on already-staged deletions — amend pattern used.
  - [x] T4 complete (commit 478f4f651; SPEC ✅ + Approved, 0 Crit/Imp; B plugin.xml registers all 8 automation refs in correct ns blocks (tabProvider in com.workflow.orchestrator ns, rest in com.intellij ns), AutomationConfigurable parentId=workflow.orchestrator, 0 ConflictDetector/BaselineCache/PollingLifecycle; konsist 5/5 GREEN, :plugin-b:buildPlugin verifyPluginStructure passed. assertTrue import now used → Task-1 unused-import nit resolved.)
  - [x] T5 complete (commit 0487a06f8; SPEC ✅ + Approved, 0 issues; git rename automation→document, package changed, locatePluginZip unchanged (already targets A's ZIP), assumeTrue guard + all 4 assertions intact, :document:test 4/4 passed 0 skipped, :automation:test green).
  - [x] T6 complete (commit ca38a0ba4; SPEC ✅ + Approved, 0 issues; root+automation CLAUDE.md + spec §21 verbatim w/ all 3 decisions [bamboo.ui coupling, bambooBuildVariableName-defer-to-2c + OSS-cut warning, sqlite-transitive correction]; DockerTagsProvider.kt + BuildLogCache.kt comment-only; detekt-clean).
  - [x] 2a GATE GREEN: module tests (core/automation/plugin-b/document/konsist) ✅; verifyPlugin ✅ (A dist: 0 automation classes + 0 sqlite); B dist jar check ✅ (173 automation + sqlite, 0 core/bamboo/okhttp); koverVerify -Pcoverage -x uiTest ✅ (floor minBound(48) holds — kept kover(:automation)); detekt: konsist+core touched files clean, document clean. GATE-FIXES committed: 34c5352f9 (plugin-b sandboxContainer isolation → fixes aggregate koverVerify Gradle-9.4 cross-task-dep), 0619aae07 (ktlint-wrap konsist test — the per-task-detekt-misses-konsist trap). ⚠ :uiTest fails ENVIRONMENTALLY (RemoteRobot retrofit/gson reflective-access on this JDK, fails at waitForIde setup before any tab assertion — NOT a carve regression; pre-existing headless limitation). ⚠ :plugin-b 12 detekt = PRE-EXISTING CompanyBSplitBehaviorTest.kt (Phase 0a, untouched by 2a) — user-deferred like :agent 73.
  - [x] 2a whole-sub-phase review (2 sonnet lenses — opus weekly-limited, sonnet floor per memory): architecture/OSS-skeptic=READY-TO-MERGE (bambooBuildVariableName [Important]=accepted/tracked 2c deferral; §21 accurate; sandbox fix sound); correctness/integration=MERGE-WITH-FIXES → 1 CRITICAL: `jira/.../PostCommitTransitionHandlerFactory.kt:69` referenced B-only `workflow.automation` group → NPE in A-standalone (pre-existing latent coupling EXPOSED by the carve). FIXED commit 2347e9426 (→ `workflow.jira`) + 2 konsist regression guards (no-A-ref-to-workflow.automation; kover(:automation)-retained). Re-verified: 0 A refs, konsist 7/7 GREEN.
  - **✅ PHASE 2a COMPLETE — 9 commits b82432abe..2347e9426. Gate GREEN. Both whole-sub-phase reviews resolved.**
  - ⚠ LESSON (carry to 2b): per-task reviews see only the carve DIFF; a removed A registration (notification group/service/EP) can be referenced by UNCHANGED A code elsewhere → NPE in A-standalone. Before 2b gate: grep ALL A modules' src/main for string/symbol refs to every removed handover id (`workflow.handover` group, moved services, HandoverConfigurable, etc.).
- [x] 2b PLAN written + reviewed: `docs/superpowers/plans/2026-06-27-plugin-split-phase2b-handover-carve.md`. 2 opus plan reviews (correctness/completeness + architecture/OSS-skeptic) both READY-WITH-FIXES → fixes folded → confirming opus re-review = READY. Key fixes: [Critical] detekt baselines are PER-MODULE (keyed by bare filename) → MUST migrate ~17 <ID> lines between core/handover baselines for moved files (else detekt RED); [Important] add-import (not repoint) + re-sort CopyrightFixCard imports; move PrServiceTest+PrServiceTemplateTest → :core (they test :core PrService); CopyrightModels in core/copyright (cohesion); Task5 fix user-facing bamboo "Inserted by Handover…" string. Confirmed sound: CopyrightFix→:core, HandoverConfigurable→:handover, PrService stays-in-A, Git4Idea-not-needed, A-standalone clean (no 2a-style orphan). BASE 2b = 2347e9426.
- [ ] 2b EXECUTE (5 tasks SDD) + gate + commit
  - [x] T1 complete (commit dec558b52; SPEC ✅ + Approved, 0 Crit/Imp; Handover default tab removed, new Phase2HandoverCarveContractTest, konsist GREEN + detekt-clean. Implementer correctly dropped unused assertTrue import [Task 3/4 re-add it]).
  - [x] T2 complete (commit c81228212; SPEC ✅ + Approved opus, 0 Crit/Imp; behavior-preserving move verified byte-equivalent via git rename sim 96-98%). CopyrightFixService+CopyrightModels→core.copyright, CopyrightFixServiceTest→core.copyright, PrServiceTest+PrServiceTemplateTest→core.bitbucket; no :core→:handover backdep; baseline migration core +22/handover −23 (net −1 = PrServiceTemplateTest ImportOrdering eliminated by same-package move). Moved files detekt-CLEAN in :core (only pre-existing PluginDiagnosticLogService/RepoConfigDefaultBranchTest debt remains). 2 cosmetic Minors for Task5: "[Handover:Copyright]" log tag + KDoc link to CopyrightFixCard in the :core service.
  - [x] T3 complete (commit 7e8801032; SPEC ✅ + Approved opus, 0 Crit/Imp). A plugin.xml 0 handover refs + 0 PreReviewService; CopyrightFixService+PrService KEPT (PrService relocated out of handover block, single reg); HandoverConfigurable+test moved :core→:handover (pure refactor, +PluginSettings import); baseline migrated core −6/handover +6; root impl(:handover) gone, kover kept; konsist 5/5, verifyPlugin SUCCESSFUL, A-jar 0 handover classes. **2a-LESSON grep: handover is a CLEAN LEAF — 0 orphaned refs (no 2a-style NPE).** 2 Minor dangling-KDoc (PluginSettings:372/374, HandoverOverrideTracker) for Task5 sweep.
  - [x] T4 complete (commit 01dbf1fc2; SPEC ✅ + Approved, 0 issues; B build implementation(:handover){isTransitive=false} [no Git4Idea]; B plugin.xml 9 handover regs (HandoverConfigurable handover.settings parentId workflow.orchestrator + 6 services + postStartup + tabProvider + notificationGroup); 4 forbidden classes absent; konsist 7/7; B jar handover=169/automation=173/core=0/okhttp=0; lockfile unchanged (no new deps)).
  - [x] T5 complete (docs commit d73228201 + controller revert e7dba5718; SPEC ✅ + Approved, 0 Crit/Imp). Root+handover CLAUDE.md + spec §22; BuildsAndHealthChecksConfigurable user-facing string de-Handover'd; WorkflowEvent/PluginSettings/HandoverOverrideTracker KDoc fixed (PullRequestCreated→:pullrequest). ⚠ CONTROLLER REVERTED the implementer's [Handover:Copyright]→[Copyright] log retag (e7dba5718): it made the copyright service an inconsistent outlier in the [Handover:*] family + broke runIde log-oracle docs + orphaned the baseline <ID> keyed to [Handover:Copyright]. Lesson: cosmetic log-tag retags ripple into QA oracle catalogs — leave them.
  - [x] 2b GATE GREEN: module tests (core/handover/plugin-b/konsist) ✅; verifyPlugin ✅; A dist 0 handover classes (core/copyright=8 expected, it's :core); B dist handover=169/automation=173/core=0/okhttp=0 ✅; koverVerify -Pcoverage -x uiTest ✅ (floor holds, kept kover(:handover)); detekt touched-files clean per-task (pre-existing :core PluginDiagnosticLogService/RepoConfigDefaultBranchTest + :plugin-b CompanyBSplitBehaviorTest debt remains, user-deferred).
  - [x] 2b whole-sub-phase review: 2 opus lenses BOTH READY-TO-MERGE (0 Crit/Imp in 2b work). architecture/OSS-skeptic: quickClipboardChips docker defaults = accepted/tracked 2c deferral (DO-NOT-CUT-before-2c). Minors folded (commit 5332b8af7): both Phase2*CarveContractTest aModules now exclude BOTH carved B modules (automation+handover) [latent false-positive guard]; Git4Idea doc wording (bundledPlugin not compileOnly); "project services" not project/app. Confirmed: CopyrightFixService byte-identical move, no :core→:handover backdep, A-standalone clean (no orphan), B jar handover=169/automation=173/core=0.
  - **✅ PHASE 2b COMPLETE — 8 commits dec558b52..5332b8af7. Gate GREEN. Both reviews READY-TO-MERGE.**
  - 2b commits: dec558b52..5332b8af7 (8 commits)
- [x] 2c PLAN written + reviewed: `docs/superpowers/plans/2026-06-27-plugin-split-phase2c-config-preset.md`. 3 opus plan reviews (correctness/completeness + architecture/OSS-skeptic + @State/persistence-skeptic [disassembled platform bytecode: BaseState list-deserialize is REPLACE not append → upgraders safe]) ALL READY-WITH-FIXES → comprehensive rewrite → confirming opus re-review = READY. KEY FIXES: reorder (blank-defaults Task2 BEFORE seeder Task3, fixes circularity); :handover:test in gate + fix HandoverConfigurableTest(docker.tag assertion); copyrightTemplate + defaultTargetBranch added as EP seams (spec-named B-preset values the draft dropped); CHIP-RESURRECTION fixed via one-shot `configPresetApplied` sentinel (set only when value-providing preset present → A-alone never stamps, B-after-A still seeds, curated-away chips not resurrected); app-level (not project-scoped) EP; val not const; rewrite PluginSettingsTest; drop same-package import in seeder. BASE 2c = 5332b8af7.
- [ ] 2c EXECUTE (5 tasks SDD) + gate + commit
  - [x] T1 complete (commit d3934fe69; SPEC ✅ + Approved, 0 Crit/Imp; ConfigPreset EP 4 fields @InternalApi app-level mirroring WorkflowConfig, DefaultConfigPreset order=MAX, declared+registered in A, konsist+SeamApiStabilityTest pinned, 8 resolver tests GREEN, verifyPlugin SUCCESSFUL. Minor: one kotlin.test.assertIs vs JUnit5 assertInstanceOf — cosmetic, passed detekt).
  - [x] T2 complete (commit 1977b6235; SPEC ✅ + Approved, 0 Crit/Imp; bambooBuildVariableName→"", NEUTRAL_QUICK_CLIPBOARD_CHIPS 5-item val in companion, init seeds it [no company chips], configPresetApplied sentinel, CURRENT_VERSION unchanged; PluginSettingsTest rewritten + HandoverConfigurableTest fixed [pre-adds docker.tag]; :core/:handover/:automation tests + verifyPlugin GREEN. Minor: a KDoc says "by simple name" but code uses qualified — fix in Task5).
  - [x] T3 complete (commit d727e48c0; SPEC ✅ + Approved opus, 0 Crit/Imp). ConfigPresetSeeder: one-shot (configPresetApplied) + provides-gate-before-sentinel (DefaultConfigPreset never stamps → B-after-A still seeds) + 4 per-field ==neutral guards; no same-package import. Wired: state hoisted, migrate→seed, [PluginSplit] logs kept. seed-after-migrate ordering load-bearing (upgraders' "develop" preserved). 4/4 tests GREEN (incl. chip-resurrection genuinely exercised), verifyPlugin SUCCESSFUL.
  - [x] T4 complete (commit bbd9ebba6; SPEC ✅ + Approved, 0 issues; CompanyBConfigPreset order=0, bamboo=DockerTagsAsJSON, 8-chip list (byte-identical to A's old init), defaultTargetBranch=develop, copyrightTemplate not overridden→null; registered in B plugin.xml com.workflow.orchestrator ns; 5/5 test + konsist + buildPlugin GREEN. Seeder now non-no-op.).
  - [x] T5 complete (commit fb6f78009; SPEC ✅ + Approved, 0 issues; spec §23 + core/CLAUDE.md EP table + ConfigPreset/Seeder section + handover/CLAUDE.md copyrightTemplate reconciled + PluginSettings.kt "by qualified name" nit; PluginSettings.kt comment-only; 3 DECISIONS recorded; all claims verified against impl).
  - [x] 2c GATE GREEN: module tests (core/automation/handover/plugin-b/konsist) ✅; verifyPlugin ✅; koverVerify -Pcoverage -x uiTest ✅ (floor holds — first run flaked on #51 timing tests [TemplateEditorCard debounce, Delegation sockets — TemplateEditorCard PASSED standalone; 2c touched NO :agent], passed on retry); B dist handover=169/automation=173/core=0 + CompanyBConfigPreset present. detekt touched-files clean per-task.
  - [x] 2c whole-sub-phase review: 2 opus lenses BOTH READY-TO-MERGE, Phase-2 DoD FULLY MET. Validated the separate @InternalApi ConfigPreset EP over widening frozen @StableApi WorkflowConfig as a correct §8 improvement; seed semantics correct across all 4 install scenarios; OSS-A free of company values. Minors folded (commit 834e46c37): PluginSettings docstrings (5-item neutral baseline; reader-fallback scope notes AutomationConfigurable display-getter exception) + spec §23 sentinel wording.
  - **✅ PHASE 2c COMPLETE — 6 commits d3934fe69..834e46c37. Gate GREEN. Both reviews READY-TO-MERGE.**
  - 2c commits: d3934fe69..834e46c37 (6 commits)
- 3 DECISIONS for user (in RESULTS): (1) one-shot sentinel (preset value changes won't re-apply to existing installs — bump a preset-version to force); (2) copyrightTemplate seam built but B returns null (company fills real header); (3) defaultTargetBranch="develop" now seeded for fresh A+B.
- [x] FINAL whole-branch review (dad8d4c7b..834e46c37, 22 commits/51 files): 2 opus lenses BOTH READY-TO-MERGE, 0 Crit/0 Imp. DoD 7/8 fully met (8th = this RESULTS doc, in progress). Confirmed: A-standalone clean of BOTH modules (no orphan imports/group-literals — the 2a PostCommitTransition orphan-class bug is the only instance, swept); B bundles both (handover=169/automation=173/core=0); 2c seeder seeds for BOTH modules correctly across all 4 install scenarios; no migration/1a conflict; konsist holistic. Minors all Phase-5-scrub (DockerTagsAsJSON in KDoc, [Handover:Copyright] log tag, stale automation doc-refs in sonar/agent) — NOT Phase-2 defects. NEW edge surfaced (DECISION 3): A-alone user on "main" who installs B gets flipped to "develop" (guard can't tell never-touched from deliberate-main) — record in RESULTS.
- [x] **PHASE 2 COMPLETE — 22 commits dad8d4c7b..834e46c37 (2a 9 + 2b 8 + 2c 6, minus overlap = 22). Gate GREEN. NOT pushed.**
- [x] RESULTS doc (§10): `docs/superpowers/specs/2026-06-27-plugin-split-phase2-RESULTS.md` (commit 328310f15) — per-sub-phase shipped, simplifications+why, gate results, consolidated runIde smoke checklist, 4 DECISIONS.
- [x] Memory updated: MEMORY.md headline → Phase 2 COMPLETE; project_plugin_split file → full Phase 2 entry + 5 reusable lessons.
- [x] Restored stashed Sonar-intention-dedup to working tree (popped cleanly, auto-merged plugin.xml, carve intact: 0 automation/handover refs). User's pre-existing uncommitted work (webview dist + gradle.properties + plugin.xml Sonar dedup) intact & NOT in any Phase-2 commit.
- [x] Verified: 0 Co-Authored-By / "Generated with Claude" trailers across all 23 Phase-2 commits (the 5 "claude" greps = CLAUDE.md filename refs in bodies).

## ✅✅ PHASE 2 COMPLETE — feature/plugin-split @ 328310f15 (23 commits b82432abe..328310f15). NOT pushed. All gates GREEN, 0 Crit/Imp across all reviews, DoD met. PENDING-USER: push + runIde GUI smoke + confirm 4 DECISIONS (see RESULTS).

## Notes
- ⚠ STASHED pre-existing change: `git stash@{0}` = "phase2-preexisting-sonar-intention-dedup" (working-tree Sonar intention-action de-dup in src/main/resources/META-INF/plugin.xml ~line 320, UNRELATED to Phase 2). Stashed so Task 2's plugin.xml edits commit cleanly. RESTORE for the user at end of Phase 2 (note in RESULTS).
- Other pre-existing changes LEFT in tree (NEVER `git add`): agent/.../webview/dist/* + gradle.properties pluginUntilBuild 253→261 bump.
- Phase 2 BASE = dad8d4c7b. Implementers `git add` ONLY their named files (no `git add -A`/`.`).
- :core/:konsist/:document tests are pure JUnit5 (one-BasePlatformTestCase-per-JVM invariant). konsist assertions use org.junit.jupiter.api.Assertions (NO kotlin.test dep).

---
## PHASE 3 (persona/skill hardening) — feature/plugin-split
BASE = 8e9bf78e2 (spec v2 commit). Plan: docs/superpowers/plans/2026-06-29-plugin-split-phase3.md (gitignored, on disk).
Implementers `git add` ONLY their named files (NEVER -A). NEVER stage the user's pre-existing webview/dist/*, gradle.properties, src/main/resources/META-INF/plugin.xml changes.
- [ ] Task 1: Site A filterByIdeContext gate + AgentConfigFilterTest
- [ ] Task 2: Sites B+C prose + 6 snapshot regens + CLAUDE.md
- [ ] Task 3: git-workflow SKILL.md trim
- [ ] Task 4: design-doc §24 + CHANGELOG
- [x] Task 1: complete (commit 31d3bd0f6, BASE 8e9bf78e2; SPEC ✅ + Approved). Predicate=supportsSpring (not Java), early-return intact, springGatedConfigs separate (assertEquals(4) safe), one-arg-per-line detekt-clean, TDD RED@:144/:158→GREEN 11/11, scope=2 files, no trailer. MINOR carry-forward M1: test3 (PyCharm) lacks `code-reviewer` presence assertion (plan-mandated; symmetry polish) → final review.
- NOTE: user commits parallel work on the branch (webview/core earlier; jira dd3d3992d between T1 and T2). Each task's review BASE = the implementer commit's PARENT (`<task-commit>^`), not my pre-dispatch HEAD, to isolate the Phase-3 diff.
- [x] Task 2: complete (commit 7b1e1a83e, BASE dd3d3992d=parent; SPEC ✅ + Approved, 0 Crit/Imp). Site B wrap exact (predicate + only 2 lines, mirrors spring-boot-engineer); Site C rewordings correct per persona type (no delegation-list ref in sub-agent bodies; conditional clause kept only in orchestrator subagent-driven SKILL); EXACTLY 6 snapshots changed w/ expected deltas (intellij-ultimate absent from diff = unchanged ✓); both CLAUDE.md sections; spawn-path untouched; full :agent:test green. MINOR carry-forward: (M2) CLAUDE.md Plan-D row long semicolon-joined sentence (readability); (M3) subagent-driven note placement could add an "omitted:" label → final review.
- [x] Task 3: complete (commit 21c788ac8, BASE 7b1e1a83e=parent; SPEC ✅ + Approved, 0 issues). git-workflow SKILL.md: 2 Atlassian sections → 1 neutral conditional 'PR & CI tasks (when integrations are configured)'; action bullets retained; Destructive Operations onward + frontmatter untouched; scope=1 file; no trailer. InstructionLoader/SkillVariant green.
- [x] Task 4: complete (commit c4048ac43, BASE 21c788ac8=parent; SPEC ✅ + Approved, 0 Crit/Imp). §24 appended verbatim after §23; CHANGELOG release-note under [Unreleased]→### Added; scope=2 files; no trailer. MINOR carry-forward (M4): CHANGELOG line not soft-wrapped ~80ch like neighbors (cosmetic; brief gave verbatim).
- ALL 4 TASKS COMPLETE + review-clean. Phase 3 commits: 31d3bd0f6 (T1), 7b1e1a83e (T2), 21c788ac8 (T3), c4048ac43 (T4). [user parallel commit dd3d3992d jira between T1/T2 — NOT Phase 3]. NEXT: full gate + whole-phase opus review.
- [x] WHOLE-PHASE opus review (8e9bf78e2..c4048ac43, 4 Phase-3 commits; jira dd3d3992d excluded): VERDICT READY-TO-MERGE, 0 Crit/0 Imp. PASS: 3-surface consistency (all supportsSpring + list-when-null; grep old-steer-phrasings=0 hits); advisory-gate intact (getCachedConfig unfiltered, by-name still runs, documented); orphan-grep clean (AgentConfigLoaderTest assertEquals(12) + PersonaToolsTest read unfiltered path → don't break); snapshot integrity exact (6 changed, 6 kept retain both lines); OSS-neutral. Minors all carry-forward/non-blocking. NEW Phase-5 flag: SpawnAgentTool.kt:320 example("security-auditor") schema string → swap to stack-neutral example in OSS scrub (out of Phase-3 scope per spec §3).
- PENDING: full build gate (:agent:test/verifyPlugin/koverVerify) running bg bdjv658is — controller verification.
- [x] FULL GATE GREEN: :agent:test ✅ (4m26s); koverVerify -Pcoverage -x uiTest ✅ (17m40s, no #51 flake first try); verifyPlugin ✅ — verdict "Compatible" (12 scheduled-removal/14 deprecated/41 experimental/17 internal API usages = PRE-EXISTING warnings, not Phase-3; RuntimeExecSharedKt internal-field is untouched). Initial verifyPlugin BUILD FAILED was :plugin-b:buildSearchableOptions (documented macOS headless-IDE trap, NOT Phase 3 — no :plugin-b file changed); passes with -x buildSearchableOptions.
- ✅✅ PHASE 3 COMPLETE — 4 commits 31d3bd0f6/7b1e1a83e/21c788ac8/c4048ac43 on feature/plugin-split. All per-task reviews Approved; whole-phase opus READY-TO-MERGE 0 Crit/0 Imp; full gate GREEN; DoD §9 met. NOT pushed. 4 cosmetic Minors + 1 Phase-5 flag (SpawnAgentTool example) carry-forward. runIde GUI smoke = Windows-coworker (macOS can't runIde).

---
## PHASE 4a (native Anthropic-direct LLM provider) — feature/plugin-split
BASE = 96dd11551 (spec-correction commit). Plan: docs/superpowers/plans/2026-06-30-plugin-split-phase4a.md (gitignored, on disk). Spec: docs/superpowers/specs/2026-06-30-plugin-split-phase4a-design.md (v2, review-corrected). Reviews folded: .superpowers/phase4/plan-review-{skeptic,completeness}.md.
INVARIANTS (carry from Phase 3): implementers `git add` ONLY their named files (NEVER -A); NEVER stage user's pre-existing webview/dist/*, gradle.properties, src/main/resources/META-INF/plugin.xml, docs/qa/*. :core tests pure JUnit5 (no BasePlatformTestCase; one-per-JVM). Each task's review BASE = implementer commit's PARENT (`<task-commit>^`) to isolate from user parallel commits. NO Co-Authored-By trailer. Canonical literal "anthropic" (not "anthropic-direct").
15 tasks. T1-T8 mostly :core (sonnet-friendly, well-specified); T9-T13 :agent integration (judgment); T11 (protocol threading) is the architecturally central one.
- [ ] Task 1: Settings + ServiceType.ANTHROPIC + DefaultWorkflowConfig arm + credential
- [ ] Task 2: AnthropicModelCatalog (static)
- [ ] Task 3: AnthropicModelCatalogService adapter + ContextManager wiring (C2)
- [ ] Task 4: Anthropic request DTOs + AnthropicRequestMapper
- [ ] Task 5: tool_use→canonical XML serializer + round-trip pin + collision guard
- [ ] Task 6: AnthropicSseParser
- [ ] Task 7: AnthropicNativeProtocol
- [ ] Task 8: AnthropicHttpTransport interface + proxy-aware client
- [ ] Task 9: AnthropicDirectBrain (:agent)
- [ ] Task 10: BrainFactory provider branch (branch-first, modelOverride, skip router)
- [ ] Task 11: provider-selected ToolProtocol threaded to all 5 sites (C1/I2)
- [ ] Task 12: provider-branch wiring (subagent model, probe, L2 chain, availableModels)
- [ ] Task 13: settings UI + in-chat picker
- [ ] Task 14: docs
- [ ] Task 15: gate + live smoke
- [x] Task 1: complete (commit d8acd29c3, BASE 96dd11551=parent; SPEC ✅ + Approved, 0 Crit/Imp). ServiceType.ANTHROPIC + ConnectionSettings.anthropicApiUrl + AgentSettings 4 provider fields + AnthropicSettingsTest (3/3 JUnit5, TDD RED→GREEN). Scope expanded to ALL 5 exhaustive when(service) sites (DefaultWorkflowConfig/AuthTestService/CachingInterceptor/MutationInvalidationInterceptor/SetupDialog) — reviewer grep-verified 5/5 patched, 0 missed (other when(serviceType) sites carry else). detekt-baseline: removed stale NoSemicolons WEB_SEARCH;-entry (semicolon moved to enum end). AuthTestService ANTHROPIC->"/v1/models" + interceptor "anthropic" tags = harmless forward-stubs (no native client yet). No user files staged. No trailer. MINOR carry (M1-4a): AgentSettings.kt missing blank line before sourcegraphChatModel KDoc (cosmetic) → final review.
- ⚠ BRANCH-HEALTH FLAG (not Phase-4a): T2 implementer reports 14 pre-existing detekt violations in non-Phase-4a files (PluginLogNamespaceFilterTest.kt, WorkflowNotificationService.kt, PluginDiagnosticLogService.kt, RepoConfigDefaultBranchTest.kt). Confirmed NOT in any Phase-4a commit. Each implementer verifies only THEIR files are detekt-clean. RESOLVE/triage at Task 15 gate (may need baseline or are non-blocking for the test gate which runs :agent:test/verifyPlugin/koverVerify, not standalone :core:detekt).
- [x] Task 2: complete (commit ff3382b13, BASE d8acd29c3=parent; SPEC ✅ + Approved, 0 Crit/Imp). AnthropicModelCatalog object: 4 triples (opus/sonnet 1M/128K, haiku 200K/64K, fable 1M/128K — Sonnet-128K pin verified), defaults+fallbackChain bare-ids, miss-fallbacks 200K/64K via named const vals, byId O(1). 15 JUnit5 tests (non-vacuous). TDD RED→GREEN. 2 files, no trailer. MINOR carry (M2-4a): object-level @Suppress(FunctionOnlyReturningConstant) → make per-function (default*()) at final review.
- [x] Task 3: complete (commit ed0ba0e24, BASE ff3382b13=parent; SPEC ✅ + Approved, 0 Crit/Imp). AnthropicModelCatalogService extends ModelCatalogService(baseUrl="",tokenProvider={null}); overrides getContextWindow→ContextWindow(1M opus)/supportsVision/supportsTools/getStatus/getLatestStreamApiVersion. ModelCatalogService.getContextWindow made `open` (1-line; base unchanged; other 4 already open). 4 selection sites via selectModelCatalog(shared)=if native anthropicModelCatalog else shared (DYNAMIC per-call llmProvider read — non-stale, mid-session flip honored). NAMED RISK CLEARED: ContextManager only calls getContextWindow (overridden); AgentLoop only supportsVision; base net-methods short-circuit on null token. :core 13 + :agent 3 source-text GREEN, TDD RED-first. 5 files (incl ModelCatalogService.kt open), no user files, no trailer. MINOR carry (M3-4a): NativeCatalogSelectionContractTest test-3 weak (windowLookup substring) — offset by :core behavioral.
- [x] Task 4: complete (commit 4fcd36aa7 + fix 4c366a4ac, BASE ed0ba0e24; 9/9 GREEN, TDD; SPEC ✅ Approved-after-fix). AnthropicDtos.kt: AnthropicRequest 7 fields (NO sampling = structural; system:List<TextBlock>=emptyList() omittable; cache_control ephemeral; ContentBlock{type,text?,source?,tool_use_id?,content?,input:JsonElement?}; ImageSource base64/media_type; AnthropicTool.input_schema; InputSchemaProperty{type,description?,enum,items}; Thinking{adaptive,display}; OutputConfig.effort) + AnthropicRequestMapper.build (system→field, tool_result, image via imageBytes(sha256), array items recursion, enumValues carried, thinking only-if-enabled). REVIEW: Important enumValues-dropped → FIXED 4c366a4ac (+enum test); Minor system:[] → FIXED (default emptyList). No user files, no trailer. ⚠ CROSS-TASK for T8: HTTP client MUST serialize with Json { explicitNulls=false } (else ContentBlock null fields litter the wire). MINOR: test noImg rename + explicit encodeToString import.
- [x] Task 5: complete (commit aace2265f + fix 79aad87a0, BASE 4c366a4ac; SPEC ✅ Approved-after-fix; 4/4 GREEN). ToolUseXmlSerializer.toXml(name,JsonObject)→canonical XML; JsonPrimitive.content raw / object-array compact JSON; COLLISION GUARD: error('collision') on non-CODE_CARRYING param value containing '<' (code params exempt). Round-trip pin exercises REAL AssistantMessageParser (4 cases). REVIEW: Important CODE_CARRYING_PARAMS duplication → FIXED 79aad87a0 (now references public AssistantMessageParser.CODE_CARRYING_PARAMS, shared w/ DialectDriftDetector). No user files, no trailer.
- ↺ REORDER: T6 (SSE parser) references AnthropicNativeProtocol().classifyStreamLine (a T7 type); T7 has no dep on T6 → doing T7 BEFORE T6. New order: …T5, T7, T6, T8…
- [x] Task 7: complete (commit 34847a943, BASE 79aad87a0; SPEC ✅ Approved 0 Crit/0 Imp/2 Minor; 9 GREEN). AnthropicNativeProtocol:NativeProtocol. INHERITANCE VERIFIED (NativeProtocol.kt:21-27): only parseToolCalls+classifyStreamLine abstract; presentTools=null + requiresDialectGuard=false + toolResultWirePrefix="" + stripPartialTag passthrough + endsWithIncompleteTag=false inherited (presentTools GENUINELY null, no override). parseToolCalls→AssistantMessageParser.parse (Option A). classifyStreamLine(String):String?→error.type (lenient JSON+try/catch→null). classifyHttpError(Int,String):String? CONCRETE (matches LlmProvider.classifyHttpError sig LlmProvider.kt:65) maps 429/529/413/400/401-403. @InternalApi consistent. 2 files, no user files, no trailer. MINOR carry: (a) KDoc says AnthropicDirectProvider/Task11 → should be AnthropicHttpClient/Task8 → FOLD FIX into T8 (T8 consumer touches this file's KDoc); (b) report said 8 tests=actually 9.
- [x] Task 6: complete (commit c2371fa29 + fix 08b52cc7f, BASE 34847a943; SPEC ✅ Approved-after-fix; 4/4 GREEN). AnthropicSseParser.parse(Sequence<String>,emitText)→Result(finishReason,usageOutputTokens,errorClass). Pure state machine: text_delta→emit; thinking ONE contiguous <thinking>…</thinking> span (open 1st delta/close content_block_stop; 2-delta test added); tool_use accumulate input_json_delta per index→toXml at message_stop (LinkedHashMap index order); message_delta→stop_reason+usage; event:error→classifyStreamLine. REVIEW: Important collision-guard-swallow → FIXED 08b52cc7f (split runCatching: JSON-parse tolerated, toXml collision PROPAGATES). 2 Minor folded. No user files, no trailer. ⚠ CROSS-TASK for T9: parse() can now THROW IllegalStateException on tool_use collision → AnthropicDirectBrain.chatStream MUST catch parse exceptions → ApiResult.Error (don't crash loop).
- [x] Task 8: complete (commit 31ead5712 + fix dfe42c7f4, BASE 08b52cc7f; SPEC ✅ Approved-after-fix; 9 MockWebServer GREEN). AnthropicHttpTransport iface (postStream(AnthropicRequest,onLine):ApiResult<Unit>) + AnthropicHttpClient(baseUrl,apiKey,timeouts,debugDir). :web triad (IdeProxy+IdeTrust); headers x-api-key/anthropic-version:2023-06-01; POST {baseUrl}/v1/messages; serialize Json{explicitNulls=false}; SSE okio readUtf8Line→onLine (raw incl blanks); error map 429→RATE_LIMITED(+retryAfterMs)/529→SERVER_ERROR/413→CONTEXT_LENGTH_EXCEEDED/400→VALIDATION/401→AUTH_FAILED/403→FORBIDDEN/IOException→NETWORK_ERROR/SocketTimeout→TIMEOUT; debug dump x-api-key REDACTED; isolated from HttpClientFactory. Folded T7 KDoc nit. REVIEW: Important no-cancellation → FIXED dfe42c7f4 (coroutineContext[Job]?.invokeOnCompletion{call.cancel()}). No user files, no trailer. ⚠ CROSS-TASK for T9: brain interruptStream()/cancelActiveRequest() must cancel the coroutine running postStream to leverage this.
- ★★ :core NATIVE STACK COMPLETE (T1-T8): settings/catalog/catalog-wiring/DTOs+mapper/serializer/protocol/SSE-parser/HTTP-transport. Now :agent integration (T9-T13).
- [x] Task 9: complete (commit 18fbc1515 + streaming-fix 079bcffe9, BASE dfe42c7f4; SPEC ✅ Approved 0 Crit/1 Imp(T8-rooted)/2 Minor; 4/4 GREEN; OPUS review). AnthropicDirectBrain:LlmBrain (:agent/ai). chatStream: PRODUCER/CONSUMER true-streaming bridge — LinkedBlockingQueue<LineMsg>(transport push→parser pull) + UNLIMITED Channel<String>(emitText→suspend onChunk); EndOfStream in finally (deadlock-free on success/error/parseError/CANCEL — opus traced 4 paths); trySend no-drop. temperature no-op; maxTokens?:catalog.maxOutput; collision→PARSE_ERROR; image via attachmentAccess.store.readBlocking+extToMime+Base64 (null-skip); chat()→chatStream(no-op onChunk); estimateTokens→TokenEstimator; interrupt→activeJob?.cancel(). Minor: no progressiveness unit test (timing-flaky, by-inspection); test3 near-tautological. 2 files, no user files, no trailer.
- ⚠ T8 FOLLOW-UP (from T9 opus review): AnthropicHttpClient cancellation NOT prompt — invokeOnCompletion(onCancelling=false) fires on FINAL completion (after blocking read ends) → call.cancel() too late; native Stop keeps streaming to message_stop/180s. FIX = onCancelling=true + ensureActive() in read loop.
- [x] T8 cancellation follow-up (from T9 opus review): complete (commit bd03f59de; 9/9 GREEN). Prompt stop: coroutineContext.ensureActive() in read loop (active-stream) + invokeOnCompletion(onCancelling=true){call.cancel()} (stalled-stream). ⚠ PHASE-5 OSS FLAG: uses @OptIn(InternalCoroutinesApi) for onCancelling=true → replace w/ stable watchdog (launch{try{awaitCancellation()}finally{call.cancel()}}) before OSS publish.
- [x] Task 14 (docs): complete (commit 5e4184782; controller-written inline while T10 ran, disjoint files). design-doc §25 (Phase 4a resolved — Option A, :core stack, provider exclusivity, 5-site protocol threading, 4b deferrals, Phase-5 InternalCoroutinesApi flag) + agent/CLAUDE.md dual-provider subsection under ## LLM API. Sourced from corrected spec. 2 files, no user files, no trailer. NOTE: written before T11-T13 land — T15 whole-phase review must sanity-check doc claims vs final code.
- ⚠ T15 GATE NOTE (detekt branch-health, NOT Phase-4a): T10 implementer wrongly regenerated agent/detekt-baseline.xml (sweeping user's parallel WRITE_TOOLS→isMutating refactoring violations in SafetyPropsCharacterizationTest/SystemPromptIdeContextTest/ToolContributionRunnerTest/etc.) — REVERTED in T10 fix. After revert, standalone `:agent:detekt` may fail on those USER-PARALLEL files (not in old baseline). This is the user's uncommitted/parallel work, NOT a Phase-4a regression. The gate (`:agent:test`/verifyPlugin/koverVerify) does not run standalone detekt (Phase 3 gate passed without it). Do NOT regenerate the baseline to "fix" it; each Phase-4a task's OWN files are detekt-clean.
- [x] Task 10: complete (commit 8a1525a48 + fix 234d42e91, BASE 5e4184782; SPEC ✅ Approved-after-fix; 12/12 GREEN; reviewed). BrainFactory native branch FIRST: modelOverride?:anthropicModel?:defaultModel(); CredentialStore.getToken(ANTHROPIC); baseUrl=anthropicApiUrl; debugDir gated; AnthropicHttpClient+AnthropicDirectBrain; 2 backward-compat ctor lambdas (attachmentAccess/sessionDebugDir={null}). AgentService: AttachmentStore HOISTED to executeTaskInternal pre-createBrain (reviewer-traced: NO double-init, SG reuses pre-inited via dead ?:, isolation preserved, currentBrainModelId both paths); wrapBrainOrSkip both sites; SG warm-up guarded !=anthropic. REVIEW: Critical baseline wholesale-regen → REVERTED 234d42e91 (==pre-T10 MATCH); BrainFactory import already correct (ktlint case-SENSITIVE empirically verified); Important test#3 ||-trivial → hardened; 2 KDoc Minors fixed. detekt: T10 files CLEAN; 129 weighted remaining = user parallel work (branch-health). No user files, no trailer.
- ⚠ LESSON: Agent isolation:worktree forks from a STALE base (worktree-agent-affdb082...forked from 38e7ee9c2 = pre-Phase-4a, NOT current HEAD). T13 worktree commit afb1c7769 re-implemented T1/T2 + edited stale AgentParentConfigurable/AgentController (both diverged HEAD↔stale-base) → UNSALVAGEABLE for cherry-pick. DISCARDED. Re-doing T13 fresh on main (sequential — safe now that T11 committed). DO NOT use isolation:worktree for tasks building on recent branch commits.
- [x] Task 11: complete (commit 63df78967, BASE 234d42e91; SPEC ✅ APPROVED 0 Crit/0 Imp/0 Minor; OPUS completeness audit; 11/11 + full :agent:test --no-build-cache GREEN). C1 FIX. Stale @Service toolProtocol val → per-task resolveActiveToolProtocol() (native→AnthropicNativeProtocol). activeToolProtocol threaded to ALL prompt-build+guard sites (orchestrator presentTools, XmlLlmProvider facade, AgentLoop, resume redaction guard, live+resume+subagent-factory MessageStateHandler). SubagentRunner hardcoded→injectable ctor param (default XML). SpawnAgentTool settable var toolProtocol pushed in executeTask BEFORE loop.run() (reviewer: ALL spawn paths funnel executeTask → no native spawn sees XML default). revertToUserMessage MessageStateHandler verified PERSISTENCE-ONLY (no presentTools/dialect-chokepoint) → correctly XML. Non-stale. Old field removed. 5 files, no user files, no baseline, no trailer.
- [x] Task 12: complete (commit 4401cf14a, BASE 0fa1cf57f; SPEC ✅ Approved 0 Crit/0 Imp/3 Minor; 4/4 GREEN). Provider-branch wiring §5.7 — all guarded llmProvider==anthropic, SG in else (no leak): C2 SpawnAgentTool sub-agent model→AnthropicModelCatalog.defaultSubagentModel() (bare sonnet)+model-param desc; I1 llmProbeUrl→anthropicApiUrl.trimEnd('/').ifBlank{null}; I2 L2→AnthropicModelCatalog.fallbackChain() (substringAfterLast('::') log harmless on bare; →BrainFactory.create handles bare); I4 BOTH formatModelsForPrompt sites→AnthropicModelCatalog.MODELS.map (availableModels:List<String>? iterates blindly=shape-OK). 3 files, no user/forbidden/baseline, no trailer. MINOR carry→T15: (a) I4 test regex aliases to I2 code; (b) I4 lambda duplicated 2 sites (DRY); (c) fable-5 no capability tags.
- [x] Task 13: complete (commit 0fa1cf57f, BASE 63df78967; SPEC ✅ Approved 0 Crit/0 Imp/1 Minor; 6/6 GREEN; clean RE-DO after worktree-stale discard). AgentParentConfigurable Native LLM Provider group (provider dropdown→llmProvider; API-key password→CredentialStore.storeToken(ANTHROPIC) NEVER XML [off-EDT + suppressAnthropicKeyChange guard]; base-url→anthropicApiUrl; model→anthropicModel; effort→anthropicEffort; thinking toggle); DialogPanel bindItem + delegate isModified/apply/reset (reviewer-traced correct). applyAnthropicProviderSettings PURE seam (headless test). AgentController picker pushInitialState/loadModelList(→loadAnthropicModelList)/changeModel/selectedModelId ALL drive anthropicModel on native (no split); SG byte-for-byte unchanged. 3 files, no forbidden, no trailer. MINOR carry→T15: blank API-key field = no-op on CredentialStore (std IJ behavior, UX foot-gun).
- ★★★ ALL 14 IMPLEMENTATION TASKS COMPLETE (T1-T14). Phase-4a commits: d8acd29c3 ff3382b13 ed0ba0e24 4fcd36aa7 4c366a4ac aace2265f 79aad87a0 34847a943 c2371fa29 08b52cc7f 31ead5712 dfe42c7f4 bd03f59de 18fbc1515 079bcffe9 5e4184782(docs) 8a1525a48 234d42e91 63df78967 0fa1cf57f 4401cf14a (+ spec corrections 96dd11551). NEXT: T15 gate + whole-phase opus review + live-smoke run-sheet.
- ★★ WHOLE-PHASE OPUS AUDIT: VERDICT READY-TO-MERGE, 0 Crit/0 Imp. Provider-exclusivity AIRTIGHT (full table: every SG-machinery site guarded llmProvider==anthropic or else-branch; xmlProvider dead, HaikuPhraseGenerator no-ops on blank sg, getSharedModelCatalog→null). Invariants PASS: no-sampling-params (structural), Option-A round-trip (collision guard + shared CODE_CARRYING_PARAMS + real-parser test), C1 threading re-verified at seam (no T12/T13 re-introduction; revert handler persistence-only), :core JUnit5 (1 cosmetic deviation), max_tokens always present. Minors ALL defer-acceptable. 
- POST-GATE FIXES TO APPLY (2 cosmetic, audit-surfaced): (1) AnthropicHttpClientTest.kt:13 `import kotlin.test.assertIs`→`org.junit.jupiter.api.Assertions.assertInstanceOf` (:core purity); (2) SpawnAgentTool.kt:332/335 model-param DOC still shows SG `anthropic::2024-10-22::...` example + ModelCache.pickSonnetNonThinking ref → add bare Anthropic-ID form (§5.7-C2 partial miss; runtime branch :706-713 already correct). M2 sub-agent api-debug parity = diagnostics-only, defer Phase-5.
- ⚠ GATE FAILED (HEAD 4401cf14a) → caught a CRITICAL the reviews+audit missed. Failures:
  * SpawnAgentToolTest (8+) ClassCastException AgentSettings + NativeProtocolThreadingContractTest "must not read llmProvider" → ROOT: T12 C2 reads AgentSettings.getInstance(project).state.llmProvider INSIDE SpawnAgentTool.kt:707 → mock-Project trap that T11 deliberately avoided (T11's contract test caught it; per-task review+grep-audit couldn't — only full-suite mock-Project run does). FIX: push native subagent model from AgentService (mirror T11 toolProtocol push) + SpawnAgentTool uses pushed field, NO AgentSettings.getInstance.
  * ToolDslSchemaParityTest: model-param DSL vs schema description DRIFT (T12 updated one not both) → make identical (also fixes audit's stale-doc Minor).
  * DefaultWorkflowConfigTest (3) NoSuchMethodError ConnectionSettings$State.<init> = build-cache/compile-avoidance trap (T1 widened ctor; stale test bytecode) — gate used --no-build-cache but NOT --rerun-tasks. NOT a code bug; re-gate with --rerun-tasks.
  * verifyPlugin + koverVerify BUILD FAILED = downstream of the test failures (depend on test). Should clear once tests pass.
  * Other agent fails (ParallelSubagentIntegration/AgentServiceModelChangeCas/Delegation*/SpawnAgentTool$SubAgentLoop+$ParallelExec) = likely cascade from the ClassCastException OR pre-existing #51 flaky — re-gate to confirm.
  → FIX dispatched (push pattern + doc parity). Then FULL re-gate with --rerun-tasks.
- ⚠ RE-GATE (732fd3d59, --rerun-tasks) analysis — separated REAL from artifact:
  * REAL #1 (CRITICAL, blocks verifyPlugin/buildPlugin): plugin-b/CompanyBWorkflowConfig.kt:13 'when must be exhaustive — add ANTHROPIC'. T1's ServiceType.ANTHROPIC broke a CROSS-MODULE exhaustive when; T1's reviewer only grep'd :core. Only verifyPlugin (compiles ALL modules) surfaces it — per-module test gates can't. FIX: ANTHROPIC->state.anthropicApiUrl (mirror DefaultWorkflowConfig). plugin-b is the only main-repo break (other modules have else; T1 fixed 5 :core sites).
  * REAL #2: ProviderBranchWiringTest:43 — trap-fix 732fd3d59 moved defaultSubagentModel() SpawnAgentTool→AgentService; T12 test greps wrong file. FIX test.
  * REAL #3: AgentServiceModelChangeCasTest:75 — T11 dialect-area edit shifted the dialectDriftSnapshot/SystemPrompt.build proximity (investigate: code regression vs test-window). Failed in BOTH gates → T11-area, not trap-fix.
  * ARTIFACT (NOT real): hundreds of platform/BasePlatformTestCase failures (AddContentRootActionTest/AgentConfigFilterTest/Monitor*/etc.) = --rerun-tasks forcing ALL platform tests fresh → per-JVM indexing-timeout CASCADE. Phase 3 ran these green INCREMENTALLY (no --rerun-tasks). MY --rerun-tasks choice polluted the signal. DefaultWorkflowConfigTest NoSuchMethodError = build-cache stale-bytecode (T1 widened ctor) — clears on clean recompile.
  → LESSON: enum-exhaustiveness must be checked CROSS-MODULE (verifyPlugin), not just the module that adds the value. Final gate = verifyPlugin (full compile) + TARGETED Phase-4a --tests (NO --rerun-tasks full suite).
  → FIX dispatched (plugin-b when + compile-all-modules sweep + 2 tests).
- ★★★ FINAL GATE GREEN (HEAD after fix 06049fda1): CORE_TEST=0 (--rerun-tasks, fresh, NoSuchMethodError cleared) · AGENT_TEST=0 (incremental, BUILD SUCCESSFUL 3m41s) · VERIFY=0 (verifyPlugin, all modules incl plugin-b compile + verifier) · KOVER=0 (koverVerify, no #51 flake, no coverage-threshold fail). DelegationTransientRetentionTest "FAILED" lines = #51 flaky real-time/socket tests passed-on-retry (build exited 0); unrelated to Phase-4a. 
- ✅✅✅ PHASE 4a COMPLETE + GATE GREEN. 14 SDD tasks + T15 gate. Whole-phase opus audit READY-TO-MERGE 0 Crit/0 Imp (provider-exclusivity airtight, all structural invariants pass). Gate caught 2 cross-cutting bugs reviews structurally couldn't (T12 mock-Project trap; T1 cross-module exhaustive-when in plugin-b) — both fixed. NOT pushed (on feature/plugin-split). Live-smoke run-sheet docs/qa/PHASE-4a-RUNIDE-SMOKE.md (coworker, Anthropic key needed). Deferred: 4b (structured-persistence/BrainRouter dissolution/multi-breakpoint cache); Phase-5 OSS scrub (InternalCoroutinesApi→watchdog). Stale worktree worktree-agent-affdb082ad23a6803 left (harmless; harness-owned).
