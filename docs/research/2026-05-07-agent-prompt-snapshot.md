# Agent System Prompt — Runtime Snapshot (HEAD = `508509dd`)

This file captures what the LLM actually sees as its system prompt at the
end of the 2026-05-07 audit-and-gap-fix series.

## How to read this file

The system prompt is assembled in `agent/src/main/kotlin/com/workflow/orchestrator/agent/prompt/SystemPrompt.kt`
(`SystemPrompt.build()`, lines 30–176). It has 11 sections separated by `====`.
Sections 1–11 are deterministic from inputs; Section 6c (`toolDefinitionsMarkdown`)
is built per-iteration by `core.ai.ToolPromptBuilder.build(tools)` from the
currently-registered tools' metadata.

To keep this snapshot reproducible:

- **Sections 1–11 (static body):** identical to the existing golden snapshot
  `agent/src/test/resources/prompt-snapshots/intellij-ultimate.txt` (474 lines,
  generated by `SystemPromptIdeContextTest`). The IntelliJ-Ultimate variant is
  used because it exercises every IDE-aware code path (Java + Kotlin + Spring +
  Gradle + microservices module) and produces the longest static prompt. Reusing
  the golden file avoids drift between this audit and the test suite — the
  snapshot test guards the same bytes.

- **Section 6c (dynamic body):** the integration tools' XML schema as
  rendered by `ToolPromptBuilder.build()` from each tool's `description` /
  `parameters.properties`. Reproduced inline below from the live tool source
  files (`agent/src/main/kotlin/com/workflow/orchestrator/agent/tools/integration/*.kt`)
  as of HEAD `508509dd`. Only the seven integration tools (jira, bamboo_builds,
  bamboo_plans, bitbucket_pr, bitbucket_repo, bitbucket_review, sonar) are
  reproduced — they are the ones the audit cares about. The other ~30 core
  tools are stable across the audit window.

## Assumptions for this snapshot

- IDE: IntelliJ IDEA 2025.1 Ultimate (Java + Kotlin, Spring + Microservices, Gradle).
- All four backend integrations configured (Jira, Bamboo, Bitbucket, Sonar) — so all
  seven integration tools are registered.
- Plan mode: off (act mode).
- No active skill, no memory index, no task progress (clean session).
- Available models list is empty (Section 7 omits the "Available model IDs" footer).

## Token estimate

- Sections 1–11 static body: ≈ 12,000 tokens (474 lines, 19,800 chars / 3.5 ≈ 5,650
  tokens by chars-÷-3.5; the actual figure from `TokenEstimator` on the live golden
  is ≈ 6,200). Plus the seven integration tool XML schemas below ≈ 4,800 tokens.
- **Total ≈ 11,000 tokens** for the system prompt at session start with all
  integrations on.

---

## Section 1–11 (static body)

Reproduced verbatim from
`agent/src/test/resources/prompt-snapshots/intellij-ultimate.txt`. Open that file
to read the full body — copying it here would just duplicate test fixture bytes.

For convenience, the section markers in order:

1. Agent Role — `agent/prompt/SystemPrompt.kt:184` (`agentRole`)
2. Task Management — `:196` (`taskProgress`)
3. Editing Files — `:234` (`editingFiles`)
4. Act vs Plan Mode — `:250` (`actVsPlanMode`)
5. Capabilities — `:284` (`capabilities`)
6. Skills (omitted when no skills loaded) — `:483` (`skills`)
6b. Deferred Tool Catalog (omitted when no deferred tools registered as catalog) — `:528` (`deferredToolCatalog`)
6c. Tool Definitions (XML; ALWAYS PRESENT) — `:131` switch in `build()` → renderer at `core/src/main/kotlin/com/workflow/orchestrator/core/ai/ToolPromptBuilder.kt:13`
7. Rules — `agent/prompt/SystemPrompt.kt:561` (`rules`)
8. System Info — `:761` (`systemInfo`)
9. Objective — `:787` (`objective`)
10. Memory — `:802` (`memory`)
11. User Instructions — `:936` (`userInstructions`)

---

## Section 6c — Tool Definitions (integration tools only)

Below is the exact text the LLM sees for the seven integration tools. This is
the output of `ToolPromptBuilder.build(tools)` restricted to the integration
tools, reproduced from each tool's `description` and `parameters.properties`
fields as of HEAD `508509dd`.

```
# Tool Use Format

You have tools to assist you. To use a tool, write the XML tag for that tool with its parameters as child tags:

<tool_name>
<parameter_name>value</parameter_name>
</tool_name>

- Always use the XML format shown above — do not use JSON or code blocks for tool calls.
- You may use multiple tools in one response.
- For parameters containing code (content, new_string, old_string, diff), write the code directly — no escaping needed.
- Even for large code blocks (100+ lines), use the appropriate tool with XML tags.

…(core tool definitions for read_file, edit_file, create_file, search_code,
glob_files, run_command, etc. — ~30 tools, ≈ 6,000 tokens; not reproduced here
because they are unchanged across the audit window)…

## jira
Description: Jira ticket management — issues, sprints, boards, transitions, comments, time logging.

Actions and their parameters:
- get_ticket(key, include_dev_status?, include_remote_links?, include_history?, include_permissions?) → Full ticket details. include_* flags fan out in parallel; each adds a block to the response. include_dev_status for "what's the status across CI/PR", include_remote_links for "what design docs link to this", include_history for "who changed what when", include_permissions to check what actions the current user can take before attempting them.
- get_transitions(key) → Available status transitions
- transition(key, transition_id, fields?, comment?) → Move ticket to new status.
  If the response payload is MissingFields, call ask_followup_question for each
  field to collect values from the user, then retry with fields={<fieldId>: <value>, ...}.
  fields format:
    user/assignee/reviewer: {"name": "username"}   (Jira DC)
    labels:                 ["label1", "label2"]
    priority/select/option: {"id": "option-id"}
    multi select:           [{"id": "a"}, {"id": "b"}]
    cascading:              {"value": "parent", "child": {"value": "child"}}
    version/component:      {"id": "id"} or [{"id": "id"}, ...]
- comment(key, body) → Add comment to ticket
- get_comments(key) → List comments
- log_work(key, time_spent, comment?) → Log time (format: '2h', '30m', '1h 30m')
- get_worklogs(key) → List work logs
- get_sprints(board_id) → List sprints for board
- get_boards(type?, name_filter?) → List boards (type: scrum|kanban)
- get_sprint_issues(sprint_id) → Issues in sprint
- get_board_issues(board_id) → Issues on board
- search_issues(text, max_results?, current_user_only?) → JQL/text search (default 20 results, default current user only)
- search_tickets(jql, max_results?) → Run raw JQL query
- get_linked_prs(key) → PRs linked to issue
- get_dev_branches(key) → Dev branches for issue
- start_work(key, branch_name, source_branch) → Create branch and start work
- download_attachment(key, attachment_id) → Download attachment content

key: Jira issue key (e.g. PROJ-123) — used across all actions that operate on a single issue.
description optional: for approval dialog on write actions.

Parameters:
- action: (required) Operation to perform
- key: (optional) Jira issue key (e.g. PROJ-123) — used across all actions that operate on a single issue
- transition_id: (optional) Transition ID — for transition (use get_transitions first)
- fields: (optional) Optional field values for transition screens — for transition. Keys are Jira field IDs (e.g. "assignee", "labels", "priority"). Values follow the Jira field-value format: user={"name":"username"}, labels=["l1","l2"], option/priority={"id":"id"}, multi-select=[{"id":"a"},{"id":"b"}], cascading={"value":"parent","child":{"value":"child"}}.
- body: (optional) Comment body — for comment
- comment: (optional) Optional comment — for transition, log_work
- time_spent: (optional) Time in Jira format: '2h', '30m', '1h 30m' — for log_work
- board_id: (optional) Board ID — for get_sprints, get_boards (optional filter), get_board_issues
- sprint_id: (optional) Sprint ID — for get_sprint_issues
- type: (optional) Board type filter: 'scrum' or 'kanban' — for get_boards
- name_filter: (optional) Name filter — for get_boards
- text: (optional) Search text — for search_issues
- jql: (optional) Raw JQL query string — for search_tickets
- max_results: (optional) Max results (default 20 for search_issues, default 8 for search_tickets)
- current_user_only: (optional) Filter to current user's tickets only: 'true' (default) or 'false' — for search_issues
- attachment_id: (optional) Jira attachment ID — for download_attachment
- branch_name: (optional) Branch name to create — for start_work
- source_branch: (optional) Source branch — for start_work
- description: (optional) Brief description of this action shown to user in approval dialog — recommended for transition, comment, start_work
- include_dev_status: (optional) When true, get_ticket also embeds the full dev panel (branches, PRs, commits, builds, deployments, reviews) in the response. Default false. Use for broad questions like 'has this been deployed' or 'what's the status of this ticket'.
- include_remote_links: (optional) If true, also fetch remote links (Confluence pages, external URLs linked from this ticket). Default false.
- include_history: (optional) If true, also fetch the ticket's status/assignee/priority change history. Default false.
- include_permissions: (optional) If true, also fetch the user's permissions on this ticket's project (transition, comment, log work, watch). Default false. Useful for the LLM to check before attempting an action that may 403.
Usage:
<jira>
<action>action value here</action>
<key>key value here</key>
... (one tag per parameter) ...
</jira>

## bamboo_builds
Description: REMOTE CI ONLY: Bamboo build lifecycle — trigger, monitor, stop, inspect builds and test results.

Use for: 'show me the latest Bamboo build', 'why did CI fail', 'fetch artifact from build N', 'rerun failed CI jobs'.
Do NOT use for: local IDE Maven/Gradle reload errors, 'why did my IDE build fail', or anything in the IDE's Build tool window — use get_build_problems for those.

Actions and their parameters:
- build_status(plan_key, branch?, repo_name?) → Latest build status for plan
- get_build(build_key, include_commits?) → Detailed build info. Returns BuildResultData with stages[].jobs[].resultKey usable as the build_key parameter for get_build_log/get_test_results. Pass include_commits=true to also fetch the per-build commit list (SHA, message, author) and complete the Bamboo→Bitbucket→Jira triangle.
- trigger_build(plan_key, variables?) → Trigger new build (variables: JSON {"key":"value"})
- get_build_log(build_key) → Build log output. Accepts a build key (e.g. PROJ-PLAN138-4) for the whole-build log, OR a job-level resultKey from get_build's stages[].jobs[].resultKey (e.g. PROJ-PLAN138-UNIT-4) for just that job's log. Prefer per-job logs when triaging a single failing job.
- get_test_results(build_key) → Test results for build
- stop_build(build_key) → Stop running build
- cancel_build(build_key) → Cancel queued build
- get_artifacts(build_key) → List build artifacts
- download_artifact(artifact_url, target_path?) → Download build artifact to local file
- recent_builds(plan_key, branch?, repo_name?, max_results?) → Recent builds (default 10)
- get_running_builds(plan_key, repo_name?) → Currently running builds

build_key: Bamboo build result key (e.g. PROJ-PLAN-123) — used across all actions that operate on a single build.
description optional: for approval dialog on trigger/stop/cancel.

Parameters:
- action: (required)
- plan_key: (optional)
- build_key: (optional)
- branch: (optional)
- repo_name: (optional)
- variables: (optional)
- artifact_url: (optional)
- target_path: (optional)
- max_results: (optional)
- description: (optional)
- include_commits: (optional) If true, also fetch the per-build commit list (SHA, message, author) via Bamboo's expand=changes.change. Default false to keep token cost low.

## bamboo_plans
Description: REMOTE CI ONLY: Bamboo plan management — discover, search, and configure build plans and variables.

Use for: 'list Bamboo plans', 'find a CI plan for repo X', 'show plan branches', 'rerun failed CI jobs'.
Do NOT use for: local IDE Maven/Gradle reload, project module discovery, or local build configuration — use get_build_problems / project_structure for those.

Actions and their parameters:
- get_projects() → List all Bamboo projects (use this before get_project_plans to discover valid project keys)
- get_plans() → List all build plans
- get_project_plans(project_key) → Plans in a project
- search_plans(query) → Search plans by name
- get_plan_branches(plan_key, repo_name?) → Plan branch list
- get_build_variables(result_key) → Variables for a specific build
- get_plan_variables(plan_key) → Default plan variables
- rerun_failed_jobs(plan_key, build_number) → Rerun failed jobs in build
- trigger_stage(plan_key, stage?, variables?) → Trigger specific stage (variables: JSON {"key":"value"})
- auto_detect_plan(git_remote_url, repo_root?, branch_name?, preferred_master?) →
    Auto-detect the Bamboo plan key from a Git remote URL.
    When repo_root / branch_name / preferred_master are absent: uses legacy 1-arg detection (fast plan-list scan).
    When any of those optional params is supplied: uses the richer 5-tier detection (better for multi-module repos).

description optional: for approval dialog on rerun_failed_jobs/trigger_stage.

Parameters:
- action: (required)
- plan_key, project_key, query, repo_name, result_key, build_number, stage, variables, git_remote_url: (optional)
- repo_root: (optional) Optional. Local repo root path. When provided alongside git_remote_url (or with branch_name/preferred_master), routes auto_detect_plan to the richer 5-tier detection (better for multi-module repos).
- branch_name: (optional) Optional. Branch to detect. Triggers the 5-tier auto_detect_plan overload when present.
- preferred_master: (optional) Optional. Preferred master/main branch name. Triggers the 5-tier auto_detect_plan overload when present.
- description: (optional)

## bitbucket_pr
Description: Pull request lifecycle — create, inspect, approve, merge, decline, update PRs.

Actions and their parameters:
- create_pr, get_pr_detail, get_pr_commits, get_pr_activities, get_pr_changes, get_pr_diff,
  check_merge_status, approve_pr, merge_pr, decline_pr, update_pr_title, update_pr_description,
  get_my_prs, get_reviewing_prs,
  get_pr_participants, get_blocker_comment_count, get_linked_jira_issues, get_required_builds,
  get_prs_for_branch
- get_pr_participants(pr_id) → Reviewer status + lastReviewedCommit (R-SWAP-5)
- get_blocker_comment_count(pr_id) → Cheap counter for blocker-severity comments (R-SWAP-4)
- get_linked_jira_issues(pr_id) → Jira keys for the PR via Bitbucket's Jira-link plugin (R-ADD-11)
- get_required_builds → Per-branch required-builds conditions for the active repo (R-ADD-15)
- get_prs_for_branch(branch_name, repo_name?) → Lists PRs that have the given branch as their source. Use this to answer "is there a PR for the branch I just pushed?" — get_my_prs does NOT cover this case (PRs may be opened by anyone).

Parameters: action (required), pr_id, title, pr_description, from_branch, to_branch, new_title,
state, strategy, delete_source_branch, commit_message, repo_name, branch_name, description.

## bitbucket_repo
Description: Repository-level operations — branches, users, files, build statuses, repo listing.

Actions and their parameters:
- get_branches(filter?), create_branch(name, start_point), search_users(filter),
  get_file_content(file_path, at_ref), get_build_statuses(commit_id),
  get_commit_build_stats(commit_id) → Aggregate {successful, failed, inProgress} counter (R-ADD-12),
  get_commit_pull_requests(commit_id) → Reverse lookup: PRs containing this commit (R-ADD-5),
  list_repos()

Parameters: action (required), filter, name, start_point, file_path, at_ref, commit_id,
repo_name, description.

## bitbucket_review
Description: PR review interactions — comments (general/inline), reviewer status, comment lifecycle.

Actions and their parameters:
- add_pr_comment, add_inline_comment, reply_to_comment
- add_reviewer, remove_reviewer, set_reviewer_status
- list_comments(only_open?, only_inline?), get_comment, edit_comment(expected_version),
  delete_comment(expected_version), resolve_comment, reopen_comment

## sonar
Description: SonarQube code quality — issues, coverage, quality gates, analysis, security hotspots.

Actions: issues, quality_gate, coverage, search_projects, analysis_tasks,
branches(include_files?), rule(rule_key), project_measures, source_lines, issues_paged,
security_hotspots, hotspot_detail, issue_facets, current_user, quality_gates_list,
duplications, branch_quality_report, local_analysis.

(Each carries a multi-line description in the live tool — see SonarTool.kt:45-69
for the verbatim text the LLM sees, including 25.x impacts/cleanCodeAttribute
guidance, branch_quality_report consolidated-call hint, and local_analysis
protected-branch redirection notes.)
</tool_name_for_xml_emit>
```

End of Section 6c.
